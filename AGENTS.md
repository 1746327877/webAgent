# AGENTS.md

本文件是本仓库的**唯一开发约定来源**：给 AI 助手与协作者看。动手前先读；改动完成后按 §6 清单自检。

> 冲突时优先级：本文件 > 其他文档 > 个人习惯。

---

## 1. 项目全貌

### 1.1 是什么

一个**本地部署的多智能体 AI 工作台**。目标是把「多模型协作 + 知识库溯源 + 可观测性」做成一个人也能跑起来、能演示、能讲清楚的完整系统，而不是只会聊天的 Demo。

使用者：在本机/内网部署，接入宿主机 Ollama；通过网页完成「建智能体 → 绑知识库/工具/MCP → 对话 → 看调用链与指标」的闭环。

### 1.2 功能模块

| 模块 | 能力 | 设计文档 |
|---|---|---|
| 会话（M1） | 会话持久化、侧边栏分组/搜索/置顶/归档、消息停止·重发·评分、标题自动生成 | `docs/设计/06` |
| 智能体（M2） | 系统提示词（支持 `{{agent.name}}` 等变量）、参数、工具绑定、版本发布与回滚 | `docs/设计/04` |
| 知识库 RAG（M3） | 上传/解析/切片/嵌入流水线、pgvector + jieba FTS + RRF 混合检索、引用溯源 | `docs/设计/05` |
| 模型调度与协作（M4） | Ollama 单槽位热切换、显存值守、`@` 接力协作、多模态图片理解 | `docs/设计/07` |
| 可观测性（M5） | 统一 spans（llm/tool/retrieval/model_switch）、HTTP 分钟级计数、`/admin` 仪表盘与会话 waterfall | `docs/设计/08` |
| 打磨与发布（M6） | 思考链、OpenAI 兼容端点 + API Key、Docker 全链路、主题/虚拟滚动、演示数据 | `docs/设计/10` |
| 扩展能力（新） | Skill（指令包注入 prompt）、MCP（手动接入 + 测试连接 + 运行时调用）、Tool（内置工具展示与绑定） | `docs/设计/12` |

### 1.3 架构与数据流

```
浏览器(React SPA)
  ├─ REST /api/v1/*        CRUD、会话、智能体、知识库、MCP…
  └─ SSE  /api/v1/sessions/{id}/messages   流式对话
        │
FastAPI 单体
  ├─ AgentRuntime   ReAct 工具循环（内置工具 + MCP 工具）
  ├─ ModelManager   Ollama 单槽位，按需 load/unload
  ├─ RAG            混合检索（向量 + 全文 + RRF）
  ├─ observability  spans / HTTP 计数
  └─ OpenAI 兼容端点 /v1/chat/completions（model=agent:{id}）
        │
数据层：PostgreSQL(pgvector) + Redis(ARQ worker) + uploads 卷
模型层：宿主机 Ollama(:11434)
```

一轮对话：SSE 建立 → 加载有效配置（智能体 + skill 注入 prompt + 工具/MCP 载荷）→ ReAct 循环（可能多轮工具调用，每次落 `tool` span）→ 流式落库 → 结束后生成标题/刷新列表。

### 1.4 技术栈

- **后端**：Python 3.12、FastAPI、SQLAlchemy 2(async)、Alembic、ARQ、Pydantic v2、`mcp` SDK、pytest + ruff。
- **前端**：React 19、TypeScript、Vite、TanStack Query、zustand、Tailwind、shadcn 风格自建组件、vitest + oxlint + tsc。
- **基础设施**：PostgreSQL 16 + pgvector、Redis、Docker Compose；模型走宿主机 Ollama。

### 1.5 当前状态与延后项

- 已实现：M1–M6 全部；扩展能力 Skill/MCP/Tool 的接入与运行时调用（Phase 1、Phase 2 均完成）。
- 已知延后项与限制：见 `README.md` 的「已知延后项」与 `docs/设计/12` 的「已知限制」；不要在未确认的情况下自行"顺手"实现它们。

### 1.6 一键启动与排障

见 `README.md` 与 `.\start.ps1`（`-Local` 本地开发 / `-Down` 停止）；后端日志 `docker compose logs -f backend`。不在此重复命令，避免两处维护。

---

## 2. 开发规范（核心）

### 2.0 目标与底线

三个目标，按优先级：**可读性 > 可维护性 > 可扩展性**（读不懂的代码谈不上后两者）。

一条底线：**不写"魔法糖"**。规范的价值在于让后来者（包括三个月后的你）能快速、确定地理解代码在做什么。

### 2.1 简洁优先

- 一个函数只做一件事；一个文件只承担一类职责。
- 文件经验阈值：**后端单文件 ≤ 400 行、前端组件 ≤ 250 行**；超过就考虑按职责拆分（如 `runtime.py` 的工具执行抽到独立模块）。不是硬性红线，但超了要能说出理由。
- 不提前抽象：**同类逻辑出现第三次**再抽公共函数；两次以内的重复是允许的。
- 不写用不到的参数、配置项、数据库字段、抽象层。YAGNI。
- 嵌套深度 ≤ 3；超了用早返回（guard clause）打平。
- 单个函数参数 ≤ 5；超过用明确的 dataclass / TypedDict / Pydantic 模型承载。

### 2.2 注释齐全

- 注释解释**为什么**（约束、坑、取舍），不是复述代码在做什么。
- 必须写注释的位置：模块头部（职责）、公开函数/组件（用途、入参、副作用）、跨越边界的地方（协议顺序、事务语义）、非直观的边界与兜底（超时、截断、降级）、正则与魔法值。
- 用中文，与现有代码一致。改动逻辑时**同步更新注释**——过时注释比没有注释更糟。
- `TODO`/`FIXME` 必须写清原因与触发条件，例如 `# TODO(m5): 待接入 retention_service，当前 spans 不清理`。
- 不写"变更日志式"注释（`// 2026-09-16 改成 X`），历史归 git。
- 不保留注释掉的死代码，需要就删（git 里有）。

### 2.3 禁止魔法糖

这里指**隐藏控制流、隐藏副作用、难以搜索**的写法。语言常规用法（解构、f-string、类型别名、列表推导）不在此列。

禁止：

- **魔术字符串/魔数直接散落**：统一提为具名常量或枚举（如 `status = "ok" | "error"` 要有集中定义）。
- **隐式副作用**：函数名看不出会写库/发请求/改全局状态的，要么改名，要么拆分。
- **全局可变状态**：模块级可变单例（缓存、标志位）必须注释生命周期与并发假设；跨请求共享要明确说明。
- **深层元编程/动态注册**：`__getattr__`、运行时改类、隐式装饰器副作用、按字符串反射调用。已有的显式注册表模式（`TOOL_REGISTRY`、`SKILL_REGISTRY`）是明确白名单，可以照用。
- **`**kwargs`/`*args` 传递业务参数**：用于透传第三方参数可以，用于自己模块间传参不行——要显式命名。
- **超长三元/链式表达式**：拆成 `if` 或中间变量，让每一步可命名。
- **前端 `any` 与字符串键**：能用类型就用类型；`Record<string, unknown>` 要说明来源；不用字符串拼接当状态机。
- **隐式约定**：例如"这个数组必须有序""这个 id 一定存在"——要么在类型/校验里保证，要么注释写明。

判断标准一句话：**如果新人不能靠 search 找到它的定义和行为，就是魔法糖。**

### 2.4 复用优先（禁止重复造轮子）

写代码前按顺序检查：

1. **仓库里有没有**：先 grep（同名函数/组件/常量、相似职责模块）。已有 `toast`、`ModelManager`、`mcp_service`、`agent_config` 这类能力不要另起炉灶。
2. **标准库 / 已有依赖能不能做**：能用已有依赖就不加新依赖。
3. **成熟实现能不能参考/直接引**：协议类（MCP）、加密、解析等优先用官方库（我们就是这么用 `mcp` SDK 的），不要手写协议。
4. 以上都不行，**再自己写**，并在 PR/回复里说明为什么。

同时**禁止**：复制粘贴一段代码改两行当新函数；同一常量在两个文件各定义一份；前端两处各写一份 fetch 逻辑（统一走 `apiFetch`/`apiJson`）。

**单一来源原则**：一份知识只存在一个地方。文档同理——命令与启动细节只在 `README.md`，本文件只引用不复制。

### 2.5 可读 / 可维护 / 可扩展

- **命名**：见名知意，不用缩写（`cfg`/`ctx` 这类行业内通用缩写可接受）；布尔用 `is_/has_/can_`；集合用复数。
- **分层固定**：路由薄（校验 + 调用服务）、服务厚（业务）、模型纯（数据结构）。不要在校验层写业务、在模型里发请求。
- **契约显式**：跨进程/跨模块的接口用 Pydantic 模型或 TS interface 固化，并与后端逐字段对齐（如 `SpanItem`、`SkillInfo` 都注释了对应后端来源）。
- **前后端一致**：接口字段命名、错误 `detail` 语义保持一致；前端类型改动要同步后端 schema。
- **可扩展靠组合**：新能力优先"新增模块 + 注册"（如再加一个内置工具就是加个 `@tool`），而不是往一个巨型 `if/elif` 里塞分支。
- **不留半成品**：功能要么完整交付（含测试），要么不开工；不允许"先提交一半以后再说"。
- **不过早优化**：先正确再快；性能优化要有测量依据（benchmark/日志），不靠"感觉会慢"。

### 2.6 依赖准入

- 新增依赖必须能回答三问：**已有依赖能否替代？维护是否活跃？为了它值不值？**
- 新依赖要写进 `pyproject.toml` / `package.json`，并在此处或回复里说明用途。
- 前端避免为单个小组件引库（如 toast 用已有 zustand 手写，而不是引 sonner）。
- 依赖升级单独成步，不与功能改动混在一起。

### 2.7 错误处理

- **不吞异常**：`except Exception` 必须有注释说明为什么安全（如"工具失败只回填错误结果，不中断生成"）；能收窄就先收窄。
- **边界统一转换**：外部/工具/模型错误转成对用户可读的结果（SSE `error` 事件、`tool_result` 的 `error` 状态、API 统一 `detail`），内部记录原始异常。
- **给可操作的提示**：错误信息说清"哪里错了、怎么修"（如 `HTTP 401（鉴权失败，检查请求头里的 Token 是否有效）`），不要只抛 `Server returned an error response`。
- **失败可降级**：外部依赖（Ollama、MCP）不可用时，功能降级而非整体 500；降级路径要有日志。

### 2.8 日志与可观测

- 统一走 `app/core/logging.py`，不要在模块里自己 `basicConfig`。
- 关键节点要有日志：外部连接（MCP 测试/调用）、工具执行失败、模型流式出错、未处理异常（带堆栈）。日志要能定位到"哪个对象、什么参数、什么结果"。
- **敏感值不入日志**：Token/密钥/请求头一律不打（见 `mcp_service._target`）。
- 新增外部调用要有 span 埋点，暴露在 `/admin`。

### 2.9 测试

- **改行为就补测试**，最低要求：新增/修改的后端逻辑有 pytest 覆盖；前端新增交互/组件有 vitest 覆盖。
- 优先"行为测试"而非实现细节；跨边界用集成测试（如 MCP 用真实 stdio 子进程验证，而不是只 mock）。
- 涉及数据模型改动时，**必须**同步 `tests/conftest.py` 的 TRUNCATE 表清单。
- 修 bug 先写能复现的测试，再修。
- 完成门槛（见 §6）：后端 `ruff` + `pytest` 全绿；前端 `tsc -b` + `vitest` 全绿。

### 2.10 前后端分工

- **系统能力（可被多方复用、涉及数据/协议/鉴权）放后端**：如 skill 清单、MCP 探测、工具注册。
- **纯展示与交互（布局、格式化、动效）放前端**。
- 同一份"事实"只有一个来源：能后端算的不要前端再算一遍，反之亦然。
- 前端不做业务规则兜底之外的重复校验（后端仍要校验）；前端校验只为体验。

### 2.11 数据与迁移

- 改模型必须配 Alembic 迁移：`uv run alembic revision --autogenerate -m "..."`，并跑 `uv run alembic check` 确认无漂移。
- 迁移要可回滚（写 `downgrade`），删除列/表前确认无引用。
- 唯一约束、级联删除（`ondelete`）要在模型里显式声明并注释语义。
- 明文存储敏感字段（如 MCP 请求头）必须在设计文档与代码注释里标注。

### 2.12 安全

- 所有用户资源查询都要带归属校验（别人的资源返回 404，不是 403，避免泄露存在性）。
- 入参校验放 Pydantic/TS 类型；外部输入（文件、URL、MCP 响应）当不可信处理。
- 上传/外链注意大小、类型、超时；不拼接命令与路径（stdio 命令来自配置，明确其信任边界）。

### 2.13 反例清单（看到就该改）

- 一个函数 200 行、一个文件 1000 行。
- `except Exception: pass`。
- 复制粘贴的"孪生函数"。
- 魔法值/字符串满屏，改一处漏一处。
- 注释在说谎（和代码不符）。
- 为一个小功能引入大依赖。
- 前端直接 `fetch` 绕过统一的鉴权封装。
- 新增表却忘了迁移，或迁移忘了同步 conftest。
- 功能无测试、无日志、无错误处理。

---

## 3. 常用命令

后端（在 `backend/`）：

```powershell
uv sync
uv run ruff check .              # lint
uv run pytest -q                 # 测试（conftest 会自建测试库）
uv run alembic upgrade head      # 应用迁移
uv run alembic check             # 模型与迁移无漂移
```

前端（在 `frontend/`）：

```powershell
pnpm i
pnpm dev                         # /api 代理到 :8000
npx tsc -b                       # 类型检查
npx vitest run                   # 测试
npx oxlint                       # lint
```

一键启动：`.\start.ps1`（`-Local` 本地开发 / `-Down` 停止）。

---

## 4. 文档约定

- 设计文档：`docs/设计/NN-标题.md`，编号递增、不复用；**先写文档再实现**。
- 实施计划：`docs/superpowers/plans/`。
- 语言用中文，与现有文档一致。

---

## 5. Obsidian 同步（硬性规则）

每次新增或修改 `docs/设计/*.md` 后，**必须**同步到 Obsidian 库：

```
E:\Obisdian_Vault\new vault\demo\多智能体平台
```

```powershell
.\scripts\sync-docs.ps1            # 同步
.\scripts\sync-docs.ps1 -DryRun    # 只预览
.\scripts\sync-docs.ps1 -Force     # 连 vault 较新的也覆盖
```

规则（由脚本保证）：

- 按**标题后缀**映射：仓库 `12-扩展能力与MCP.md` ↔ vault `14-扩展能力与MCP.md`；vault 已有编号不改，新增文档自动取下一个空号。
- **vault 较新的文件不覆盖**（默认只提示，`-Force` 才覆盖，覆盖前自动 `.bak-时间戳`）。
- 只同步 `docs/设计`；`plans/`、`README`、本文件不同步。
- 路径与源目录在 `scripts/obsidian-sync.json`（UTF-8）；脚本保持 ASCII，避免 PowerShell 5.1 读取非 BOM 中文出错。

**完成定义**：设计文档改动 = 仓库文件已更新 **且** `sync-docs.ps1` 已执行 **且** 回复里说明同步结果。

---

## 6. 提交与推送（GitHub）

- 每个可交付改动完成后：`git add -A` → `.\scripts\check-secrets.ps1` → commit → `git push` 到 `origin` 当前分支。
- **绝不提交隐私/凭据**：
  - `.env`（含 `JWT_SECRET`/`PG_PWD` 等）已在 `.gitignore`，不要移除该忽略；只提交 `.env.example` 占位值。
  - 不把 Token / API Key / 密码写进代码、测试、文档、脚本或注释（测试用假值，如 `sk-test`）。
  - MCP 请求头等敏感值只存数据库或环境变量，不落仓库。
  - 产物与运行时数据（`output/`、`uploads/`、`dist/`、`*.log`）不提交。
- push 前**必须**运行 `.\scripts\check-secrets.ps1`；脚本会扫描待提交文件的名称与内容特征，报错就修掉再推。
- 提交信息用祈使句简述"改了什么"，可带 scope，例如 `feat(capabilities): 支持 MCP 工具绑定`。

## 7. 变更完成清单

- [ ] 遵守 §2：简洁、注释齐全、无魔法糖、复用优先、有测试
- [ ] 后端：`ruff` 与 `pytest` 通过；涉及模型有迁移且 `alembic check` 干净
- [ ] 前端：`tsc -b` 与 `vitest` 通过；未引入无关依赖
- [ ] 若改了 `docs/设计/*.md`：已运行 `.\scripts\sync-docs.ps1` 并确认结果
- [ ] 已运行 `.\scripts\check-secrets.ps1`，提交并推送到 GitHub
- [ ] 回复里说明：改了什么、怎么验证的、是否需要 `docker compose up -d --build`
