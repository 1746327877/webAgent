# M3：知识库 RAG 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让知识库成为可用的检索增强能力：文件上传 → 异步解析/切片/嵌入流水线（状态机）→ **pgvector + jieba 全文 + RRF 混合检索** → 聊天时自动注入检索上下文并**流式输出引用（citation）**，回答可点开原文；同时清偿 M2 终审的快速遗留项。

**Architecture:** 新增 `knowledge_bases/documents/chunks/agent_kbs` 数据表（pgvector `Vector(1024)` + `tsvector` 生成列）与 `spans` 基础表（M5 可观测性的前置基础设施，本里程碑先用于检索记录）；ARQ（Redis）执行异步 ingest；`app/ai/rag/` 提供解析、切片、嵌入、混合检索；运行时在流式前执行检索、注入 system 上下文、发 `citation` 事件并写检索 span；前端新增 `/kb` 管理页、智能体知识库绑定区、聊天引用 UI（角标/依据折叠/侧滑抽屉）。

**Tech Stack:** M0-M2 全部沿用。后端新增依赖：`arq`、`jieba`、`pymupdf`、`python-docx`、`pgvector`、`python-multipart`；前端零新依赖（复用 shadcn）。嵌入模型：`bge-m3`（Ollama，1024 维）。

**Spec（设计文档）:** `docs/设计/05-知识库RAG.md`（流水线/切片/中文 FTS/RRF/引用）、`02-数据库设计.md`（knowledge_bases/documents/chunks/agent_kbs/spans）、`06-会话与消息流.md`（检索注入与 citation）、`09-前端交互设计.md`（引用 UI）。

## Global Constraints

- 沿用 M0-M2 全部约束：API 前缀 `/api/v1`；Windows + PowerShell；中文 UI；提交前缀 `feat:/fix:/chore:` 且每任务提交一次；测试输出 pristine；JWT/bcrypt/PG16+pgvector/Redis；`ensure_ascii=False`。
- **分支策略**：`feat/m3-rag` 从 `feat/m2-agents` 尖端（`61af318`）创建（堆叠；合并顺序 M0→M1→M2→M3）。M3 计划文档为分支首个提交。
- SSE 新增事件：`citation {message_id, ref, chunk_id, source, page, score, snippet}`（在 token 之前发）；其余沿用（message_start/token/thinking/tool_call/tool_result/error/done）。
- `chunks.embedding vector(1024)`（bge-m3）；`chunks.tsv` 由 `to_tsvector('simple', content_tokens)` 生成（`content_tokens` = jieba 分词后空格拼接，中文 FTS 方案）。
- 混合检索：向量通道（`<=>` 余弦，top 40）+ 全文通道（`tsquery`/`ts_rank`，top 40）→ **UNION ALL + 按 chunk id 分组 RRF 融合**（`1/(60+rank)`）→ 取 top_k；`channel_hits` 保留可解释性。
- 切片参数：`chunk_size=512` 字符、`overlap=64` 字符（近似 token 口径）；Markdown/文本优先按标题行（`#{1,6} `）切段，其次按空行段落，最后硬切；`meta={page?, heading_path?}`。
- 引用注入：system 消息追加 `[知识库检索结果] [1] (来源: file pN) ...` 并要求模型用 `[1][2]` 标注；无命中时不注入。
- 上传限制：类型白名单 `pdf/md/txt/docx`，单文件 ≤20MB；存储名 `uploads/{uuid}.{ext}`。
- 新增后端依赖仅上表 6 个；前端零新增 npm 依赖。
- 所有命令在对应目录执行（后端 `D:\code\webAgent\backend`、前端 `D:\code\webAgent\frontend`、git 在仓库根）。

---

### Task 1: M2 快速遗留修复（契约收紧 / usage 累加 / 工具取消检查 / tool_name / 注释）

**Files:**
- Modify: `backend/app/schemas/agent.py`（ModelConfig 收紧）
- Modify: `backend/app/services/agent_service.py`（`_tool_slugs` 排序、`set_tools` 去重）
- Modify: `backend/app/ai/runtime.py`（usage 合并、工具执行间取消检查、tool 消息补 `tool_name`、陈旧注释修正）
- Test: `backend/tests/test_m2_followups.py`（3 条）

**Interfaces:**
- Produces: `ModelConfig.model` 非空（min_length=1）、`provider: Literal["ollama"]`；`_merge_usage(acc, new) -> dict`；工具执行间的取消检查（`CANCEL_FLAGS` 在每轮工具执行前 pop 检查）。

- [ ] **Step 0: 创建 M3 分支并提交计划文档**

```powershell
cd D:\code\webAgent
git switch -c feat/m3-rag
git add docs/superpowers/plans/2026-09-11-M3-知识库RAG.md
git commit -m "docs: add m3 implementation plan"
```

- [ ] **Step 1: 写失败测试 `backend/tests/test_m2_followups.py`**

```python
from app.schemas.agent import ModelConfig


def test_model_config_rejects_empty_model():
    import pytest
    from pydantic import ValidationError

    with pytest.raises(ValidationError):
        ModelConfig(model="")
    with pytest.raises(ValidationError):
        ModelConfig(model="m", provider="openai")


async def test_usage_merges_across_rounds(client, auth_headers, session_maker):
    from app.ai.deps import get_provider
    from app.main import app
    from tests.fake_provider import FakeProvider

    agent = {
        "name": "U",
        "system_prompt": "",
        "model_config": {"model": "m"},
        "tags": [],
        "examples": [],
    }
    aid = (await client.post("/api/v1/agents", json=agent, headers=auth_headers)).json()["id"]
    s = (await client.post("/api/v1/sessions", json={"agent_id": aid}, headers=auth_headers)).json()
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    provider = FakeProvider(
        [
            [
                ("tool_call", {"id": "c1", "name": "time_now", "args": {}}),
                ("usage", {"prompt_tokens": 10, "completion_tokens": 1, "total_ms": 100}),
            ],
            [
                ("token", {"delta": "ok"}),
                ("usage", {"prompt_tokens": 20, "completion_tokens": 2, "total_ms": 50}),
            ],
        ]
    )
    app.dependency_overrides[get_provider] = lambda: provider
    await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    msgs = (await client.get(f"/api/v1/sessions/{s['id']}/messages", headers=auth_headers)).json()
    usage = msgs[1]["usage"]
    assert usage["prompt_tokens"] == 30 and usage["completion_tokens"] == 3
    assert usage["total_ms"] == 150


async def test_set_tools_deduplicates(client, auth_headers, session_maker):
    from app.ai.tools import sync_tools

    async with session_maker() as db:
        await sync_tools(db)
    agent = {
        "name": "D",
        "system_prompt": "",
        "model_config": {"model": "m"},
        "tags": [],
        "examples": [],
    }
    aid = (await client.post("/api/v1/agents", json=agent, headers=auth_headers)).json()["id"]
    r = await client.put(
        f"/api/v1/agents/{aid}/tools",
        json={"slugs": ["time_now", "time_now"]},
        headers=auth_headers,
    )
    assert r.status_code == 200 and r.json()["slugs"] == ["time_now"]
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_m2_followups.py -v`
Expected: FAIL（空 model 可建 / provider 未限 / usage 仅末轮 / 重复 slug 500）

- [ ] **Step 3: 实现**

`schemas/agent.py`：

```python
from typing import Literal
...
class ModelConfig(BaseModel):
    provider: Literal["ollama"] = "ollama"
    model: str = Field(min_length=1)
    ...
```

`agent_service._tool_slugs` 查询追加 `.order_by(Tool.slug)`；`set_tools` 首行 `slugs = list(dict.fromkeys(slugs))`。

`runtime.py`：

```python
def _merge_usage(acc: dict | None, new: dict | None) -> dict | None:
    if new is None:
        return acc
    if acc is None:
        return dict(new)
    for key in ("prompt_tokens", "completion_tokens", "total_ms"):
        a, b = acc.get(key), new.get(key)
        if b is not None:
            acc[key] = (a or 0) + b
    if acc.get("first_token_ms") is None:
        acc["first_token_ms"] = new.get("first_token_ms")
    return acc
```

usage 处理改为 `usage = _merge_usage(usage, ev.payload)`；工具执行循环中、每个工具执行前检查取消：

```python
            cancelled_during_tools = False
            for call in tool_calls:
                if CANCEL_FLAGS.pop(assistant_id, False):
                    status = "stopped"
                    cancelled_during_tools = True
                    break
                ...（原执行逻辑）
            if cancelled_during_tools:
                break
```

（消息/块顺序与既有一致：assistant(tool_calls) 先于 tool 结果；被取消时不再追加后续轮消息。）
tool 消息追加补 `"tool_name": call["name"]`；修正 `_load_history` 上方关于 「created_at 排序」的陈旧注释为 seq 口径。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 70 passed：67 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "fix(backend): m2 followups - strict model config, usage merge, tool cancel check"
```

---

### Task 2: 知识库数据模型 + spans 基础表 + 迁移

**Files:**
- Create: `backend/app/models/knowledge.py`（KnowledgeBase / Document / Chunk / AgentKB）
- Create: `backend/app/models/span.py`（Span）
- Modify: `backend/app/models/__init__.py`
- Modify: `backend/tests/conftest.py`（TRUNCATE 扩展）
- Create: `backend/alembic/versions/<auto>_m3_knowledge_spans.py`
- Modify: `backend/pyproject.toml`（依赖：`pgvector`）
- Test: `backend/tests/test_m3_models.py`

**Interfaces:**
- Produces: `KnowledgeBase/Document/Chunk/AgentKB/Span` ORM；`Chunk.embedding` 为 `pgvector.sqlalchemy.Vector(1024)`；`Chunk.tsv` 生成列（`to_tsvector('simple', content_tokens)`）。

- [ ] **Step 1: 写失败测试 `backend/tests/test_m3_models.py`**

```python
import uuid

from sqlalchemy import select, text

from app.core.security import hash_password
from app.models import Agent, AgentKB, Chunk, Document, KnowledgeBase, Span, User


async def _user(db) -> User:
    u = User(
        id=uuid.uuid4(),
        username="alice",
        email="alice@example.com",
        password_hash=hash_password("Passw0rd!"),
    )
    db.add(u)
    await db.flush()
    return u


async def test_kb_document_chunk_roundtrip(session_maker):
    async with session_maker() as db:
        u = await _user(db)
        kb = KnowledgeBase(owner_id=u.id, name="Java 资料")
        db.add(kb)
        await db.flush()
        doc = Document(kb_id=kb.id, filename="a.md", file_type="md", size_bytes=10)
        db.add(doc)
        await db.flush()
        vec = [0.0] * 1024
        vec[0] = 1.0
        chunk = Chunk(
            document_id=doc.id,
            kb_id=kb.id,
            content="并发编程要点",
            content_tokens="并发 编程 要点",
            chunk_index=0,
            embedding=vec,
            meta={"heading_path": "Java/Multithreading"},
        )
        db.add(chunk)
        await db.commit()
        got = await db.scalar(select(Chunk).where(Chunk.id == chunk.id))
        assert got.content == "并发编程要点"
        assert len(list(got.embedding)) == 1024
        # tsv 由分词列生成：simple 配置下 "并发" 可命中
        hit = await db.scalar(
            text("SELECT id FROM chunks WHERE tsv @@ plainto_tsquery('simple', :q)"),
            {"q": "并发"},
        )
        assert hit == chunk.id


async def test_agent_kb_binding_and_span(session_maker):
    async with session_maker() as db:
        u = await _user(db)
        agent = Agent(owner_id=u.id, name="A", system_prompt="", model_config={})
        kb = KnowledgeBase(owner_id=u.id, name="KB")
        db.add_all([agent, kb])
        await db.flush()
        db.add(AgentKB(agent_id=agent.id, kb_id=kb.id, top_k=3, score_threshold=0.2))
        db.add(
            Span(
                trace_id=uuid.uuid4(),
                type="retrieval",
                name="kb检索",
                input={"query": "x"},
                output={"ids": ["c1"]},
                started_at=__import__("datetime").datetime.now(__import__("datetime").UTC),
                status="ok",
            )
        )
        await db.commit()
        binding = await db.scalar(select(AgentKB).where(AgentKB.agent_id == agent.id))
        assert binding.top_k == 3
        span = await db.scalar(select(Span).where(Span.type == "retrieval"))
        assert span.output["ids"] == ["c1"]
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_m3_models.py -v`
Expected: FAIL（ImportError / 模型不存在）

- [ ] **Step 3: 实现模型**

`backend/app/models/knowledge.py`：

```python
import uuid
from datetime import datetime

from pgvector.sqlalchemy import Vector
from sqlalchemy import DateTime, ForeignKey, Integer, String, Text, func, text
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import Base


class KnowledgeBase(Base):
    __tablename__ = "knowledge_bases"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    owner_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), index=True
    )
    name: Mapped[str] = mapped_column(String(64))
    description: Mapped[str | None] = mapped_column(Text)
    embedding_model: Mapped[str] = mapped_column(String(64), default="bge-m3")
    chunk_size: Mapped[int] = mapped_column(Integer, default=512)
    chunk_overlap: Mapped[int] = mapped_column(Integer, default=64)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )


class Document(Base):
    __tablename__ = "documents"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    kb_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("knowledge_bases.id", ondelete="CASCADE"), index=True
    )
    filename: Mapped[str] = mapped_column(String(255))
    file_type: Mapped[str] = mapped_column(String(16))
    size_bytes: Mapped[int] = mapped_column()
    status: Mapped[str] = mapped_column(String(16), default="pending")
    error: Mapped[str | None] = mapped_column(Text)
    chunk_count: Mapped[int] = mapped_column(Integer, default=0)
    meta: Mapped[dict] = mapped_column(JSONB, default=dict)
    uploaded_by: Mapped[uuid.UUID | None] = mapped_column(ForeignKey("users.id"), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )


class Chunk(Base):
    __tablename__ = "chunks"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    document_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("documents.id", ondelete="CASCADE"), index=True
    )
    kb_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("knowledge_bases.id", ondelete="CASCADE"), index=True
    )
    content: Mapped[str] = mapped_column(Text)
    content_tokens: Mapped[str] = mapped_column(Text)
    chunk_index: Mapped[int] = mapped_column(Integer)
    token_count: Mapped[int] = mapped_column(Integer, default=0)
    meta: Mapped[dict] = mapped_column(JSONB, default=dict)
    tsv: Mapped[str] = mapped_column(
        Text,
        __import__("sqlalchemy").Computed("to_tsvector('simple', content_tokens)", persisted=True),
    )
    embedding: Mapped[list] = mapped_column(Vector(1024))
```

> 注意：上面 `tsv` 的写法用 `Computed` 表达生成列。实现者请规范为：
> ```python
> from sqlalchemy import Computed
> tsv: Mapped[str] = mapped_column(Text, Computed("to_tsvector('simple', content_tokens)", persisted=True))
> ```
> 并确认 autogenerate 生成 `sa.Computed(...)`；若 autogenerate 未识别，手工在迁移里写 `sa.Column('tsv', postgresql.TSVECTOR(), sa.Computed("to_tsvector('simple', content_tokens)", persisted=True), nullable=True)`。

`AgentKB` 与 `Span`：

```python
class AgentKB(Base):
    __tablename__ = "agent_kbs"

    agent_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("agents.id", ondelete="CASCADE"), primary_key=True
    )
    kb_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("knowledge_bases.id", ondelete="CASCADE"), primary_key=True
    )
    top_k: Mapped[int] = mapped_column(Integer, default=5)
    score_threshold: Mapped[float] = mapped_column(default=0.3)
```

```python
class Span(Base):
    __tablename__ = "spans"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    trace_id: Mapped[uuid.UUID] = mapped_column(index=True)
    parent_span_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    session_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    message_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    agent_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    user_id: Mapped[uuid.UUID | None] = mapped_column(nullable=True)
    type: Mapped[str] = mapped_column(String(16))
    name: Mapped[str | None] = mapped_column(String(128))
    input: Mapped[dict | None] = mapped_column(JSONB)
    output: Mapped[dict | None] = mapped_column(JSONB)
    status: Mapped[str] = mapped_column(String(16), default="ok")
    error: Mapped[str | None] = mapped_column(Text)
    prompt_tokens: Mapped[int | None] = mapped_column(Integer)
    completion_tokens: Mapped[int | None] = mapped_column(Integer)
    model: Mapped[str | None] = mapped_column(String(64))
    started_at: Mapped[datetime] = mapped_column(DateTime(timezone=True))
    ended_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    duration_ms: Mapped[int | None] = mapped_column(Integer)
```

（`Span` 单独放 `backend/app/models/span.py`；`knowledge.py` 放 KB 四表。`__init__` 全部导出。）

- [ ] **Step 4: conftest + 迁移**

`conftest.py` TRUNCATE：

```python
        await c.execute(
            text(
                "TRUNCATE chunks, documents, knowledge_bases, agent_kbs, spans,"
                " attachments, messages, sessions, refresh_tokens, users, agents,"
                " agent_versions, agent_tools, tools CASCADE"
            )
        )
```

Run: `uv run alembic revision --autogenerate -m "m3 knowledge and spans"`
手工确认：
- 生成列 `tsv`（若缺：`sa.Column('tsv', postgresql.TSVECTOR(), sa.Computed("to_tsvector('simple', content_tokens)", persisted=True))`）
- 索引：`CREATE INDEX ON chunks USING hnsw (embedding vector_cosine_ops)` 与 `USING gin (tsv)`（autogenerate 通常不识别 ops 类，需手工 `op.execute`）：

```python
    op.execute("CREATE INDEX ix_chunks_embedding_hnsw ON chunks USING hnsw (embedding vector_cosine_ops)")
    op.execute("CREATE INDEX ix_chunks_tsv_gin ON chunks USING gin (tsv)")
```

downgrade 先 drop 两个索引再删表。
Run: `uv run alembic upgrade head`；`uv run alembic check`（零漂移）；`docker compose exec postgres psql -U webagent -d webagent -c "\d chunks"` 确认 vector(1024)、生成列、双索引。

- [ ] **Step 5: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 72 passed：70 + 2）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): knowledge and spans data model with vector+fts indexes"
```

---

### Task 3: 知识库 CRUD API + 文件上传 + ARQ 基建

**Files:**
- Modify: `backend/pyproject.toml`（`arq`、`jieba`、`pymupdf`、`python-docx`、`python-multipart`）
- Modify: `backend/app/core/config.py`（`redis_url`、`upload_dir`、`embedding_model`）
- Create: `backend/app/workers/__init__.py`、`backend/app/workers/settings.py`
- Create: `backend/app/schemas/knowledge.py`、`backend/app/services/kb_service.py`、`backend/app/api/v1/kbs.py`
- Modify: `backend/app/api/v1/router.py`
- Test: `backend/tests/test_kbs_api.py`（5 条）

**Interfaces:**
- Produces: `get_owned_kb(db, user, kb_id)`（404）、`get_owned_document(db, user, document_id)`；`POST/GET/PATCH/DELETE /kbs`、`POST /kbs/{id}/documents`（multipart，落盘 `uploads/`，建 document 行 status=pending，`enqueue_ingest(document_id)`）、`GET /kbs/{id}/documents`、`DELETE /kbs/{id}/documents/{doc_id}`、`POST /kbs/{id}/documents/{doc_id}/retry`。
- Produces: `enqueue_ingest(document_id)`（模块级函数，测试可 monkeypatch）；`WorkerSettings`（`arq app.workers.settings.WorkerSettings` 启动）。
- 上传白名单 `{pdf,md,txt,docx}`；>20MB → 413。

- [ ] **Step 1: 写失败测试 `backend/tests/test_kbs_api.py`**

```python
import io

from app.api.v1 import kbs as kbs_api


async def test_kb_crud_and_ownership(client, auth_headers):
    r = await client.post("/api/v1/kbs", json={"name": "Java 资料"}, headers=auth_headers)
    assert r.status_code == 201 and r.json()["name"] == "Java 资料"
    kid = r.json()["id"]
    lst = await client.get("/api/v1/kbs", headers=auth_headers)
    assert [k["id"] for k in lst.json()] == [kid]

    from tests.test_sessions_api import make_user

    other = await make_user(client, "bob")
    assert (await client.get(f"/api/v1/kbs/{kid}", headers=other)).status_code == 404
    assert (await client.delete(f"/api/v1/kbs/{kid}", headers=other)).status_code == 404
    assert (await client.delete(f"/api/v1/kbs/{kid}", headers=auth_headers)).status_code == 204


async def test_upload_enqueues_and_lists(client, auth_headers, session_maker, monkeypatch, tmp_path):
    calls: list[str] = []
    monkeypatch.setattr(kbs_api, "enqueue_ingest", lambda doc_id: calls.append(str(doc_id)))
    monkeypatch.setattr(kbs_api.settings, "upload_dir", str(tmp_path))

    kid = (await client.post("/api/v1/kbs", json={"name": "K"}, headers=auth_headers)).json()["id"]
    files = {"file": ("notes.md", io.BytesIO("标题\n正文".encode()), "text/markdown")}
    r = await client.post(f"/api/v1/kbs/{kid}/documents", files=files, headers=auth_headers)
    assert r.status_code == 201
    doc = r.json()
    assert doc["status"] == "pending" and doc["filename"] == "notes.md"
    assert calls == [doc["id"]]

    lst = await client.get(f"/api/v1/kbs/{kid}/documents", headers=auth_headers)
    assert [d["id"] for d in lst.json()] == [doc["id"]]


async def test_upload_rejects_bad_type_and_size(client, auth_headers, monkeypatch, tmp_path):
    monkeypatch.setattr(kbs_api.settings, "upload_dir", str(tmp_path))
    kid = (await client.post("/api/v1/kbs", json={"name": "K"}, headers=auth_headers)).json()["id"]
    bad = {"file": ("x.exe", io.BytesIO(b"x"), "application/octet-stream")}
    assert (await client.post(f"/api/v1/kbs/{kid}/documents", files=bad, headers=auth_headers)).status_code == 415


async def test_delete_document_and_retry(client, auth_headers, monkeypatch, tmp_path, session_maker):
    monkeypatch.setattr(kbs_api.settings, "upload_dir", str(tmp_path))
    calls: list[str] = []
    monkeypatch.setattr(kbs_api, "enqueue_ingest", lambda doc_id: calls.append(str(doc_id)))
    kid = (await client.post("/api/v1/kbs", json={"name": "K"}, headers=auth_headers)).json()["id"]
    files = {"file": ("a.md", io.BytesIO(b"x"), "text/markdown")}
    doc = (await client.post(f"/api/v1/kbs/{kid}/documents", files=files, headers=auth_headers)).json()

    # 伪装失败态后重试
    async with session_maker() as db:
        from app.models import Document

        row = await db.get(Document, doc["id"])
        row.status = "failed"
        row.error = "boom"
        await db.commit()
    r = await client.post(
        f"/api/v1/kbs/{kid}/documents/{doc['id']}/retry", headers=auth_headers
    )
    assert r.status_code == 200 and r.json()["status"] == "pending"
    assert calls[-1] == doc["id"]

    d = await client.delete(f"/api/v1/kbs/{kid}/documents/{doc['id']}", headers=auth_headers)
    assert d.status_code == 204


async def test_worker_settings_shape():
    from app.workers.settings import WorkerSettings

    assert WorkerSettings.redis_settings is not None
    assert len(WorkerSettings.functions) >= 1
```

- [ ] **Step 2: 运行确认失败（404/模块缺失）**

Run: `uv run pytest tests/test_kbs_api.py -v`
Expected: FAIL

- [ ] **Step 3: 实现**

`pyproject.toml` dependencies 追加：

```toml
    "arq>=0.26",
    "jieba>=0.42",
    "pymupdf>=1.25",
    "python-docx>=1.1",
    "python-multipart>=0.0.12",
    "pgvector>=0.3",
```

`config.py` 追加：

```python
    redis_url: str = "redis://localhost:6379/0"
    upload_dir: str = "./uploads"
    embedding_model: str = "bge-m3"
```

`app/workers/settings.py`：

```python
from arq.connections import RedisSettings

from app.core.config import settings


async def ingest_job(ctx, document_id: str) -> None:
    from app.ai.rag.pipeline import run_ingest

    await run_ingest(document_id)


class WorkerSettings:
    functions = [ingest_job]
    redis_settings = RedisSettings.from_dsn(settings.redis_url)
```

（`app/ai/rag/pipeline.py` 在 Task 4 提供 `run_ingest`；本任务允许先写占位 `run_ingest` 抛 NotImplementedError？不允许占位——把 `ingest_job` 改为**延迟导入**：`from app.ai.rag.pipeline import run_ingest` 放函数体内，本任务内 pipeline 尚未存在时 worker 启动不会失败；Task 4 落地后生效。测试只断言 `WorkerSettings` 形状，不启动 worker。）

`schemas/knowledge.py`：

```python
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field


class KBCreateIn(BaseModel):
    name: str = Field(min_length=1, max_length=64)
    description: str | None = None


class KBOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    name: str
    description: str | None
    embedding_model: str
    chunk_size: int
    chunk_overlap: int
    created_at: datetime


class DocumentOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    kb_id: uuid.UUID
    filename: str
    file_type: str
    size_bytes: int
    status: str
    error: str | None
    chunk_count: int
    created_at: datetime
```

`services/kb_service.py`：`get_owned_kb` / `list_kbs` / `create_kb` / `delete_kb` / `list_documents` / `get_owned_document`（均按 owner 过滤，404「知识库不存在」/「文档不存在」）；`create_document(db, kb, *, name, ext, size, user, stored_name)`——**把 `stored_name` 写入 `meta`（`meta={"stored_name": stored_name}`）**，供 ingest 按名取文件。

`api/v1/kbs.py` 关键端点：

```python
ALLOWED = {"pdf", "md", "txt", "docx"}
MAX_BYTES = 20 * 1024 * 1024


def enqueue_ingest(document_id) -> None:
    """生产环境入 ARQ；测试 monkeypatch。"""
    import asyncio

    from arq import create_pool
    from arq.connections import RedisSettings

    from app.core.config import settings

    async def _run():
        pool = await create_pool(RedisSettings.from_dsn(settings.redis_url))
        await pool.enqueue_job("ingest_job", str(document_id))

    asyncio.get_event_loop().create_task(_run())


@router.post("/{kid}/documents", response_model=DocumentOut, status_code=201)
async def upload_document(
    kid: uuid.UUID,
    file: UploadFile = File(...),
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    kb = await kb_service.get_owned_kb(db, user, kid)
    ext = (file.filename or "").rsplit(".", 1)[-1].lower()
    if ext not in ALLOWED:
        raise HTTPException(status_code=415, detail="不支持的文件类型")
    content = await file.read()
    if len(content) > MAX_BYTES:
        raise HTTPException(status_code=413, detail="文件超过 20MB")
    name = f"{uuid.uuid4()}.{ext}"
    path = Path(settings.upload_dir) / name
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_bytes(content)
    doc = await kb_service.create_document(
        db, kb, name=file.filename, ext=ext, size=len(content), user=user, stored_name=name
    )
    enqueue_ingest(doc.id)
    return doc
```

（`enqueue_ingest` 的生产实现可简化为 fire-and-forget task；实现者可自行用 `asyncio.create_task` 包装，保持测试 monkeypatch 点不变。retry 端点重置 `status=pending`、`error=None` 后再 enqueue。）

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv sync && uv run pytest -v`（期望 77 passed：72 + 5）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): kb crud, upload and arq worker scaffold"
```

---

### Task 4: Ingest 流水线（解析 / 切片 / jieba 分词 / 嵌入 / 状态机）

**Files:**
- Create: `backend/app/ai/rag/__init__.py`、`backend/app/ai/rag/parsers.py`、`backend/app/ai/rag/splitter.py`、`backend/app/ai/rag/pipeline.py`
- Create: `backend/tests/test_rag_pipeline.py`、`backend/tests/fixtures/java.md`（小样例）
- Test: 3 条

**Interfaces:**
- Produces: `extract_text(path: Path, file_type: str) -> list[tuple[int | None, str]]`（页号或 None + 文本）；`split_text(text: str, size: int = 512, overlap: int = 64) -> list[str]`（标题优先/段落后硬切）；`run_ingest(document_id: str, *, embedder=None, upload_dir=None) -> None`（状态机 + 落库 + span 前置不上；`embedder` 为 `async (texts: list[str]) -> list[list[float]]`，默认走 Ollama bge-m3）。
- Consumes: `Document/Chunk/KnowledgeBase` 模型；`jieba`。

- [ ] **Step 1: 写失败测试**

`backend/tests/test_rag_pipeline.py`：

```python
import uuid
from pathlib import Path

import pytest
from sqlalchemy import select

from app.models import Chunk, Document, KnowledgeBase, User
from app.ai.rag.splitter import split_text


def test_splitter_prefers_headings_and_overlaps():
    text = "# 标题一\n第一段内容。" * 1 + "\n\n" + "第二段。" * 200
    chunks = split_text(text, size=100, overlap=10)
    assert len(chunks) >= 2
    assert chunks[0].startswith("# 标题一")
    assert all(len(c) <= 130 for c in chunks)  # 允许切分余量


def test_splitter_hard_split():
    chunks = split_text("字" * 250, size=100, overlap=20)
    assert len(chunks) >= 3


async def _seed_doc(session_maker, upload_dir: Path, content: str = "# 标题\n内容。\n\n第二段。") -> tuple:
    async with session_maker() as db:
        u = User(
            id=uuid.uuid4(),
            username="alice",
            email="alice@example.com",
            password_hash="x",
        )
        db.add(u)
        await db.flush()
        kb = KnowledgeBase(owner_id=u.id, name="K")
        db.add(kb)
        await db.flush()
        path = upload_dir / f"{uuid.uuid4()}.md"
        path.write_text(content, encoding="utf-8")
        doc = Document(
            kb_id=kb.id, filename="a.md", file_type="md", size_bytes=len(content), meta={"stored_name": path.name}
        )
        db.add(doc)
        await db.commit()
        return str(doc.id), str(kb.id)


async def test_run_ingest_ready_and_chunks(session_maker, tmp_path):
    from app.ai.rag.pipeline import run_ingest

    doc_id, kb_id = await _seed_doc(session_maker, tmp_path)

    async def fake_embedder(texts: list[str]) -> list[list[float]]:
        return [[0.01 * i] * 1024 for i, _ in enumerate(texts)]

    await run_ingest(doc_id, embedder=fake_embedder, upload_dir=str(tmp_path))
    async with session_maker() as db:
        doc = await db.get(Document, uuid.UUID(doc_id))
        assert doc.status == "ready" and doc.chunk_count >= 1
        chunks = (await db.scalars(select(Chunk).where(Chunk.kb_id == uuid.UUID(kb_id)))).all()
        assert chunks and all(" " in c.content_tokens for c in chunks)
        assert all(len(list(c.embedding)) == 1024 for c in chunks)


async def test_run_ingest_failure_sets_failed(session_maker, tmp_path):
    from app.ai.rag.pipeline import run_ingest

    doc_id, _ = await _seed_doc(session_maker, tmp_path)

    async def broken_embedder(texts):  # noqa: ARG001
        raise RuntimeError("embed boom")

    await run_ingest(doc_id, embedder=broken_embedder, upload_dir=str(tmp_path))
    async with session_maker() as db:
        doc = await db.get(Document, uuid.UUID(doc_id))
        assert doc.status == "failed" and "embed boom" in (doc.error or "")
```

> 测试计数：splitter 2 + ingest 2 = 4（比预估 +3 多 1，最终以实际为准）。

- [ ] **Step 2: 运行确认失败（模块不存在）**

Run: `uv run pytest tests/test_rag_pipeline.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现**

`parsers.py`：

```python
from pathlib import Path


def extract_text(path: Path, file_type: str) -> list[tuple[int | None, str]]:
    if file_type == "pdf":
        import fitz

        out = []
        with fitz.open(path) as pdf:
            for i, page in enumerate(pdf):
                out.append((i + 1, page.get_text("text")))
        return out
    if file_type == "docx":
        import docx

        d = docx.Document(str(path))
        return [(None, "\n".join(p.text for p in d.paragraphs))]
    return [(None, path.read_text(encoding="utf-8", errors="ignore"))]
```

`splitter.py`：

```python
import re

HEADING_RE = re.compile(r"^#{1,6}\s")


def _hard_split(block: str, size: int, overlap: int) -> list[str]:
    chunks, start = [], 0
    while start < len(block):
        chunks.append(block[start : start + size])
        start += max(1, size - overlap)
    return chunks


def split_text(text: str, size: int = 512, overlap: int = 64) -> list[str]:
    text = text.strip()
    if not text:
        return []
    blocks: list[str] = []
    current: list[str] = []
    for para in text.split("\n\n"):
        para = para.strip()
        if not para:
            continue
        if HEADING_RE.match(para) and current:
            blocks.append("\n\n".join(current))
            current = [para]
        else:
            current.append(para)
        if sum(len(p) for p in current) >= size:
            blocks.append("\n\n".join(current))
            current = []
    if current:
        blocks.append("\n\n".join(current))

    chunks: list[str] = []
    for block in blocks:
        if len(block) <= size:
            chunks.append(block)
        else:
            chunks.extend(_hard_split(block, size, overlap))
    return chunks
```

`pipeline.py`：

```python
import uuid
from pathlib import Path

import jieba
from sqlalchemy import delete, select

from app.core.config import settings
from app.core.db import SessionLocal
from app.models import Chunk, Document

BATCH = 64


async def _default_embedder(texts: list[str]) -> list[list[float]]:
    from app.ai.providers.ollama import OllamaProvider

    provider = OllamaProvider(settings.ollama_base_url)
    try:
        return await provider.embed(texts, settings.embedding_model)
    finally:
        await provider.aclose()


async def _set_status(db, doc: Document, status: str, *, error: str | None = None) -> None:
    doc.status = status
    doc.error = error
    await db.commit()


async def run_ingest(
    document_id: str,
    *,
    embedder=None,
    upload_dir: str | None = None,
) -> None:
    embed = embedder or _default_embedder
    base = Path(upload_dir or settings.upload_dir)
    async with SessionLocal() as db:
        doc = await db.get(Document, uuid.UUID(document_id))
        if doc is None:
            return
        try:
            await _set_status(db, doc, "parsing")
            stored = (doc.meta or {}).get("stored_name")
            path = base / stored if stored else None
            if path is None or not path.exists():
                raise FileNotFoundError("上传文件丢失")
            from app.ai.rag.parsers import extract_text
            from app.ai.rag.splitter import split_text

            pages = extract_text(path, doc.file_type)
            meta_pages = doc.meta or {}
            await _set_status(db, doc, "chunking")
            pieces: list[tuple[str, dict]] = []
            for page_no, page_text in pages:
                for piece in split_text(page_text):
                    pieces.append((piece, {"page": page_no}))
            if not pieces:
                raise ValueError("解析结果为空（可能是扫描件）")

            await _set_status(db, doc, "embedding")
            await db.execute(delete(Chunk).where(Chunk.document_id == doc.id))
            index = 0
            for start in range(0, len(pieces), BATCH):
                batch = pieces[start : start + BATCH]
                vectors = await embed([p[0] for p in batch])
                for (content, meta), vec in zip(batch, vectors, strict=True):
                    db.add(
                        Chunk(
                            document_id=doc.id,
                            kb_id=doc.kb_id,
                            content=content,
                            content_tokens=" ".join(jieba.lcut(content)),
                            chunk_index=index,
                            embedding=vec,
                            meta=meta,
                            token_count=len(content),
                        )
                    )
                    index += 1
                await db.flush()
            doc.chunk_count = len(pieces)
            doc.meta = {**meta_pages, "pages": len(pages)}
            await _set_status(db, doc, "ready")
        except Exception as exc:  # noqa: BLE001
            await db.rollback()
            doc = await db.get(Document, uuid.UUID(document_id))
            if doc is not None:
                await _set_status(db, doc, "failed", error=str(exc)[:500])
```

> 实现注意：
> 1. 文件名落盘规则为 `{uuid}.{ext}`，而 `document_id` 是新 uuid（两者不同）——**在 `Document.meta` 里保存 `stored_name`**，ingest 按 `meta["stored_name"]` 读取（KB 上传端点在 T3 已有 meta 字段）。若 T3 未存，请在本任务一并把上传端点改为 `meta={"stored_name": name}`，并让本管线优先读该值。


- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 81 passed：77 + 4）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): ingest pipeline with parsers, splitter and embeddings"
```

---

### Task 5: 混合检索（向量 + 全文 + RRF）+ kb_search 真实实现

**Files:**
- Create: `backend/app/ai/rag/retrieval.py`
- Modify: `backend/app/ai/tools/builtins.py`（kb_search 描述更新为真实能力；handler 保留兜底文案，运行时将拦截）
- Test: `backend/tests/test_retrieval.py`（3 条）

**Interfaces:**
- Produces: `RetrievedChunk`（dataclass：`id, content, source, page, rrf_score, channel_hits`）；`async hybrid_search(db, kb_ids, query, top_k, embedder) -> list[RetrievedChunk]`；`format_context(chunks) -> str`（`[知识库检索结果] [1] (来源: a.md p2) ...` 结构）；`jieba_tokens(query) -> str`。
- Consumes: `Chunk/Document`；`jieba`；pgvector 距离运算。

- [ ] **Step 1: 写失败测试 `backend/tests/test_retrieval.py`**

```python
import uuid

from sqlalchemy import text as sql_text


async def _seed(session_maker):
    from app.models import Chunk, Document, KnowledgeBase, User

    async with session_maker() as db:
        u = User(
            id=uuid.uuid4(), username="alice", email="a@e.com", password_hash="x"
        )
        db.add(u)
        await db.flush()
        kb = KnowledgeBase(owner_id=u.id, name="K")
        db.add(kb)
        await db.flush()
        doc = Document(kb_id=kb.id, filename="java.md", file_type="md", size_bytes=1)
        db.add(doc)
        await db.flush()
        v_semantic = [0.0] * 1024
        v_semantic[0] = 1.0  # 与查询 [1,0,...] 最相似
        v_keyword = [0.0] * 1024
        v_keyword[1] = 1.0  # 与查询正交，但含关键词
        v_other = [0.0] * 1024
        v_other[2] = 1.0
        db.add_all(
            [
                Chunk(document_id=doc.id, kb_id=kb.id, content="完全语义相关", content_tokens="完全 语义 相关",
                      chunk_index=0, embedding=v_semantic),
                Chunk(document_id=doc.id, kb_id=kb.id, content="并发编程关键词命中", content_tokens="并发 编程 关键词 命中",
                      chunk_index=1, embedding=v_keyword),
                Chunk(document_id=doc.id, kb_id=kb.id, content="无关内容", content_tokens="无关 内容",
                      chunk_index=2, embedding=v_other),
            ]
        )
        await db.commit()
        return str(kb.id), str(doc.id)


async def test_hybrid_search_rrf_and_channel_hits(session_maker):
    from app.ai.rag.retrieval import hybrid_search

    kb_id, _ = await _seed(session_maker)

    async def query_embedder(texts: list[str]) -> list[list[float]]:
        vec = [0.0] * 1024
        vec[0] = 1.0
        return [vec for _ in texts]

    chunks = await hybrid_search(
        session_maker,
        [uuid.UUID(kb_id)],
        "并发 编程",
        top_k=3,
        embedder=query_embedder,
    )
    assert len(chunks) == 3
    by_content = {c.content: c for c in chunks}
    # 语义第一：向量通道 rank1；关键词条：全文通道 rank1 → 双通道命中
    assert by_content["并发编程关键词命中"].channel_hits == 2
    assert by_content["完全语义相关"].channel_hits == 1
    assert chunks[0].source == "java.md"


async def test_hybrid_search_empty_kbs(session_maker):
    from app.ai.rag.retrieval import hybrid_search

    async def emb(texts):
        return [[0.0] * 1024 for _ in texts]

    assert await hybrid_search(session_maker, [], "x", top_k=5, embedder=emb) == []


async def test_format_context_numbers_sources(session_maker):
    from app.ai.rag.retrieval import RetrievedChunk, format_context

    ctx = format_context(
        [RetrievedChunk(id="c1", content="片段", source="a.md", page=2, rrf_score=0.5, channel_hits=2)]
    )
    assert "[1]" in ctx and "a.md" in ctx and "p2" in ctx
```

- [ ] **Step 2: 运行确认失败（模块不存在）**

Run: `uv run pytest tests/test_retrieval.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 `retrieval.py`**

```python
import json
import uuid
from dataclasses import dataclass

import jieba
from sqlalchemy import text
from sqlalchemy.ext.asyncio import async_sessionmaker, AsyncSession

TOP_CHANNEL = 40
RRF_K = 60


@dataclass
class RetrievedChunk:
    id: str
    content: str
    source: str
    page: int | None
    rrf_score: float
    channel_hits: int


def jieba_tokens(query: str) -> str:
    return " ".join(jieba.lcut(query))


def _vec_literal(vector: list[float]) -> str:
    return "[" + ",".join(f"{v:.8f}" for v in vector) + "]"


async def hybrid_search(
    session_maker: async_sessionmaker[AsyncSession],
    kb_ids: list[uuid.UUID],
    query: str,
    top_k: int,
    embedder,
):
    if not kb_ids:
        return []
    vectors = await embedder([query])
    qvec = _vec_literal(vectors[0])
    tokens = jieba_tokens(query)

    sql = text(
        """
        WITH semantic AS (
            SELECT id, ROW_NUMBER() OVER (ORDER BY embedding <=> CAST(:qvec AS vector)) AS r
            FROM chunks WHERE kb_id = ANY(:kb_ids)
            ORDER BY embedding <=> CAST(:qvec AS vector) LIMIT :chan
        ),
        fulltext AS (
            SELECT c.id, ROW_NUMBER() OVER (ORDER BY ts_rank(c.tsv, q) DESC) AS r
            FROM chunks c, plainto_tsquery('simple', :tokens) q
            WHERE c.kb_id = ANY(:kb_ids) AND c.tsv @@ q
            ORDER BY ts_rank(c.tsv, q) DESC LIMIT :chan
        ),
        fused AS (
            SELECT id, SUM(1.0 / (:rrf + r)) AS rrf_score, count(*) AS channel_hits
            FROM (SELECT id, r FROM semantic UNION ALL SELECT id, r FROM fulltext) t
            GROUP BY id
        )
        SELECT c.id, c.content, c.meta, d.filename, f.rrf_score, f.channel_hits
        FROM fused f JOIN chunks c ON c.id = f.id JOIN documents d ON d.id = c.document_id
        ORDER BY f.rrf_score DESC LIMIT :top_k
        """
    )
    async with session_maker() as db:
        rows = (
            await db.execute(
                sql,
                {
                    "qvec": qvec,
                    "kb_ids": kb_ids,
                    "tokens": tokens,
                    "chan": TOP_CHANNEL,
                    "rrf": RRF_K,
                    "top_k": top_k,
                },
            )
        ).all()
    out = []
    for row in rows:
        meta = row.meta or {}
        out.append(
            RetrievedChunk(
                id=str(row.id),
                content=row.content,
                source=row.filename,
                page=meta.get("page"),
                rrf_score=float(row.rrf_score),
                channel_hits=int(row.channel_hits),
            )
        )
    return out


def format_context(chunks: list[RetrievedChunk]) -> str:
    lines = ["[知识库检索结果]"]
    for i, c in enumerate(chunks, 1):
        loc = f" p{c.page}" if c.page else ""
        snippet = c.content[:300].replace("\n", " ")
        lines.append(f"[{i}] (来源: {c.source}{loc}) {snippet}")
    lines.append("回答时请用 [1][2] 形式标注引用；检索结果未覆盖时明确说明。")
    return "\n".join(lines)
```

（`kb_ids` 传 `uuid.UUID` 列表；`ANY(:kb_ids)` 由 asyncpg 以 uuid[] 处理。）

`builtins.py` 的 `kb_search` 描述改为「在智能体绑定的知识库中检索相关文档片段」，handler 保留「未绑定知识库或检索不可用」兜底文案（运行时将拦截为真实检索）。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 84 passed：81 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): hybrid retrieval with rrf fusion"
```

---

### Task 6: 运行时检索注入 + citation 事件/块 + 检索 span

**Files:**
- Modify: `backend/app/ai/runtime.py`（检索注入、citation、span、kb_search 拦截）
- Modify: `backend/tests/fake_provider.py`（`embed` 返回 1024 维确定性向量，可配置）
- Test: `backend/tests/test_rag_runtime.py`（2 条）

**Interfaces:**
- Produces: `run_generation` 新行为——当会话智能体绑定了 KB：先检索（查询=当前用户消息；`top_k` 取绑定值），命中则 system 上下文追加 `format_context`、发 `citation` 事件（`{message_id, ref, chunk_id, source, page, score, snippet}`）、assistant blocks 前置 citation 块、写 `retrieval` span（trace_id 复用 message 的 trace 概念：本里程碑用 `assistant_id` 作为 trace_id）。
- Produces: `_execute_tool` 的 `kb_search` 拦截（真实检索 + 结果格式化，preview 含 `[1]`）。

- [ ] **Step 1: 扩展 FakeProvider 并写失败测试**

`fake_provider.py`：`embed(texts, model)` 改为返回确定性 1024 维（例如 `[float((hash(t) % 100)) / 100] + [0.0]*1023`），并支持：构造参数 `embed_vectors: list[list[float]] | None` 覆盖；**实例属性 `embed_fn: Callable | None`（设置后 embed 调用它）**。

`backend/tests/test_rag_runtime.py`：

```python
import uuid

from app.ai.deps import get_provider
from app.main import app
from tests.fake_provider import FakeProvider


async def _prepare(client, auth_headers, session_maker):
    from app.models import AgentKB, Chunk, Document, KnowledgeBase

    aid = (
        await client.post(
            "/api/v1/agents",
            json={"name": "RAG", "system_prompt": "", "model_config": {"model": "m"}, "tags": [], "examples": []},
            headers=auth_headers,
        )
    ).json()["id"]
    s = (await client.post("/api/v1/sessions", json={"agent_id": aid}, headers=auth_headers)).json()
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    async with session_maker() as db:
        user = (await client.get("/api/v1/auth/me", headers=auth_headers)).json()
        kb = KnowledgeBase(owner_id=uuid.UUID(user["id"]), name="KB")
        db.add(kb)
        await db.flush()
        doc = Document(kb_id=kb.id, filename="java.md", file_type="md", size_bytes=1)
        db.add(doc)
        await db.flush()
        vec = [0.0] * 1024
        vec[0] = 1.0
        db.add(
            Chunk(
                document_id=doc.id,
                kb_id=kb.id,
                content="Java 并发编程的核心是线程与锁",
                content_tokens="Java 并发 编程 的 核心 是 线程 与 锁",
                chunk_index=0,
                embedding=vec,
                meta={"page": 12},
            )
        )
        db.add(AgentKB(agent_id=uuid.UUID(aid), kb_id=kb.id, top_k=3))
        await db.commit()
    return aid, s["id"]


async def test_runtime_injects_context_and_citations(client, auth_headers, session_maker):
    aid, sid = await _prepare(client, auth_headers, session_maker)

    async def emb(texts):
        vec = [0.0] * 1024
        vec[0] = 1.0
        return [vec for _ in texts]

    provider = FakeProvider([("token", {"delta": "并发与锁"})], embed_vectors=None)
    provider.embed_fn = emb  # 对应 FakeProvider 的 embed_fn 属性
    app.dependency_overrides[get_provider] = lambda: provider

    r = await client.post(
        f"/api/v1/sessions/{sid}/messages", json={"content": "Java 并发编程要点？"}, headers=auth_headers
    )
    body = r.text
    assert "event: citation" in body
    assert '"source": "java.md"' in body and '"ref": 1' in body
    req = provider.requests[0]
    sys_msg = req.messages[0]
    assert sys_msg["role"] == "system" and "[知识库检索结果]" in sys_msg["content"]
    assert "[1]" in sys_msg["content"]

    msgs = (await client.get(f"/api/v1/sessions/{sid}/messages", headers=auth_headers)).json()
    types = [b["type"] for b in msgs[1]["blocks"]]
    assert types[0] == "citation"

    from sqlalchemy import select

    from app.models import Span

    async with session_maker() as db:
        span = (await db.scalars(select(Span).where(Span.type == "retrieval"))).first()
        assert span is not None and span.agent_id == uuid.UUID(aid)
        assert span.status == "ok" and span.input["query"]


async def test_no_kb_no_citation(client, auth_headers):
    aid = (
        await client.post(
            "/api/v1/agents",
            json={"name": "P", "system_prompt": "", "model_config": {"model": "m"}, "tags": [], "examples": []},
            headers=auth_headers,
        )
    ).json()["id"]
    s = (await client.post("/api/v1/sessions", json={"agent_id": aid}, headers=auth_headers)).json()
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    from app.ai.deps import get_provider as gp

    app.dependency_overrides[gp] = lambda: FakeProvider([("token", {"delta": "x"})])
    r = await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    assert "event: citation" not in r.text
```

> 测试替身说明：Task 6 实现者可按实现方式调整 `FakeProvider` 的 embed 注入方式（构造参数或属性），但测试骨架与断言保持不变；`client.get("/api/v1/auth/me")` 返回用户 id 用于建 KB 归属。

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_rag_runtime.py -v`
Expected: FAIL（无 citation / 无注入）

- [ ] **Step 3: 实现（runtime 检索段）**

在 `run_generation` 中、`message_start` 已发出且 `blocks` 初始化之后、第一次 `provider.chat_stream` 之前：

```python
        retrieval_chunks: list = []
        if cfg.agent_id is not None and user_content is not None:
            retrieval_chunks = await _retrieve_for_agent(
                db, cfg.agent_id, user_content, assistant_id=assistant_id, session_id=session.id
            )

        if retrieval_chunks:
            from app.ai.rag.retrieval import format_context

            context = format_context(retrieval_chunks)
            if cfg.system_prompt:
                messages[0]["content"] = cfg.system_prompt + "\n\n" + context
            else:
                messages.insert(0, {"role": "system", "content": context})
            citation_blocks = [
                {
                    "type": "citation",
                    "ref": i,
                    "chunk_id": c.id,
                    "source": c.source,
                    "page": c.page,
                    "score": round(c.rrf_score, 4),
                    "snippet": c.content[:200],
                }
                for i, c in enumerate(retrieval_chunks, 1)
            ]
            blocks.extend(citation_blocks)
            for block in citation_blocks:
                yield sse(
                    "citation",
                    {"message_id": str(assistant_id), **{k: v for k, v in block.items() if k != "type"}},
                )
```

`_retrieve_for_agent` 与 `_execute_tool` 的 kb_search 拦截（同一实现同时服务两条路径）：

```python
async def _retrieve_for_agent(db, agent_id, query, *, assistant_id, session_id, top_k: int | None = None):
    """按智能体绑定的 KB 检索；命中返回 RetrievedChunk 列表；写 retrieval span。"""
    import time

    from app.ai.rag.retrieval import hybrid_search
    from app.core.db import SessionLocal
    from app.models import AgentKB, KnowledgeBase
    from app.observability.spans import record_span

    bindings = (
        await db.scalars(select(AgentKB).where(AgentKB.agent_id == agent_id))
    ).all()
    if not bindings:
        return []
    kb_ids = [b.kb_id for b in bindings]
    limit = top_k or max((b.top_k for b in bindings), default=5)

    async def embedder(texts: list[str]):
        from app.ai.providers.ollama import OllamaProvider
        from app.core.config import settings

        provider = OllamaProvider(settings.ollama_base_url)
        try:
            return await provider.embed(texts, settings.embedding_model)
        finally:
            await provider.aclose()

    started = time.monotonic()
    try:
        chunks = await hybrid_search(SessionLocal, kb_ids, query, limit, embedder)
        status = "ok"
        err = None
    except Exception as exc:  # noqa: BLE001
        chunks, status, err = [], "error", str(exc)[:300]
    await record_span(
        trace_id=assistant_id,
        type="retrieval",
        name="kb检索",
        session_id=session_id,
        agent_id=agent_id,
        input={"query": query, "kb_ids": [str(k) for k in kb_ids]},
        output={"chunks": [{"id": c.id, "source": c.source, "score": c.rrf_score} for c in chunks]},
        status=status,
        error=err,
        started=started,
    )
    return chunks
```

`app/observability/__init__.py` + `spans.py`（新文件，Task 6 内）：

```python
import time
import uuid
from datetime import datetime, timezone


async def record_span(
    *,
    trace_id,
    type: str,
    name: str,
    session_id=None,
    agent_id=None,
    message_id=None,
    input=None,
    output=None,
    status: str = "ok",
    error: str | None = None,
    started: float | None = None,
    ended: float | None = None,
) -> None:
    from app.core.db import SessionLocal
    from app.models import Span

    s = started if started is not None else time.monotonic()
    e = ended if ended is not None else time.monotonic()
    async with SessionLocal() as db:
        db.add(
            Span(
                trace_id=trace_id,
                type=type,
                name=name,
                session_id=session_id,
                agent_id=agent_id,
                message_id=message_id,
                input=input,
                output=output,
                status=status,
                error=error,
                started_at=datetime.now(timezone.utc),
                ended_at=datetime.now(timezone.utc),
                duration_ms=int((e - s) * 1000),
            )
        )
        await db.commit()
```

> 测试环境注意：`record_span` 用 `SessionLocal`（dev 连接）会在测试中写错库——**`record_span(db, ...)` 必须接受请求级 `AsyncSession` 并在其上写 Span**：把 `record_span` 改为接受 `db: AsyncSession` 参数（runtime 传请求会话；`hybrid_search` 也改为接受 `db` 或 `session_maker` 可注入——Task 5 已用 `session_maker`，runtime 把 `session_maker` 从 `run_generation` 可用来源注入：`run_generation` 已有 `session_factory` 参数，可在其缺省时用 `SessionLocal`）。实现者请统一：**检索链路所有 DB 访问都走可注入的 session factory**，保证测试断言能读到 span。

`_execute_tool` 的 kb_search 分支：`_execute_tool(name, args, *, agent_id, db)`，当 `name == "kb_search"`：`query = parsed.get("query", "")`、`top_k = parsed.get("top_k")` → `_retrieve_for_agent(...)` → 结果格式化为带 `[n]` 的文本；无绑定/无命中 → 返回占位文案。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 86 passed：84 + 2）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): runtime retrieval injection with citations and spans"
```

---

### Task 7: 前端知识库管理页（列表 / 详情 / 上传 / 状态轮询）

**Files:**
- Modify: `frontend/src/lib/api.ts`（**FormData 兼容**：`init.body instanceof FormData` 时不设置 `Content-Type`）
- Create: `frontend/src/api/kbs.ts`、`frontend/src/pages/KbPage.tsx`、`frontend/src/pages/KbDetailPage.tsx`、`frontend/src/components/kb/DocumentTable.tsx`、`frontend/src/components/kb/__tests__/DocumentTable.test.tsx`
- Modify: `frontend/src/App.tsx`（路由 `/kb`、`/kb/:kbId`）、`frontend/src/components/sidebar/SessionSidebar.tsx`（底部入口「📚 知识库」）

**Interfaces:**
- Produces: hooks `useKbs/useCreateKb/useDeleteKb/useDocuments(kbId)/useUploadDocument(kbId)/useDeleteDocument/useRetryDocument`；`KbItem`/`DocumentItem` 类型（字段对齐后端 `KBOut/DocumentOut`）。
- Produces: 上传（file input，`.pdf,.md,.txt,.docx`）、文档状态徽章（pending/parsing/chunking/embedding→「处理中」，ready→「就绪」，failed→「失败」+ 重试按钮）、删除确认、列表页新建/删除 KB。
- 轮询：文档列表 query 在存在非终态时 `refetchInterval: 2000`。

- [ ] **Step 1: 写失败测试 `DocumentTable.test.tsx`**

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { expect, test, vi } from "vitest";
import DocumentTable from "@/components/kb/DocumentTable";

const DOCS = [
  { id: "d1", kb_id: "k1", filename: "a.md", file_type: "md", size_bytes: 1, status: "ready", error: null, chunk_count: 3, created_at: "2026-09-12T00:00:00Z" },
  { id: "d2", kb_id: "k1", filename: "b.pdf", file_type: "pdf", size_bytes: 2, status: "failed", error: "扫描件", chunk_count: 0, created_at: "2026-09-12T00:00:00Z" },
];

test("状态徽章与失败重试", async () => {
  const onRetry = vi.fn();
  render(<DocumentTable documents={DOCS} onRetry={onRetry} onDelete={vi.fn()} />);
  expect(screen.getByText("就绪")).toBeInTheDocument();
  expect(screen.getByText("失败")).toBeInTheDocument();
  await userEvent.click(screen.getByRole("button", { name: "重试" }));
  expect(onRetry).toHaveBeenCalledWith("d2");
});

test("处理中状态显示", () => {
  render(
    <DocumentTable
      documents={[{ ...DOCS[0], id: "d3", status: "embedding" }]}
      onRetry={vi.fn()}
      onDelete={vi.fn()}
    />,
  );
  expect(screen.getByText("处理中")).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行确认失败（模块不存在）**

Run: `pnpm run test -- --run src/components/kb/__tests__/DocumentTable.test.tsx`
Expected: FAIL

- [ ] **Step 3: 实现**

`api.ts` 的 `doFetch` 修正：

```ts
  if (init.body && !(init.body instanceof FormData) && !headers.has("Content-Type")) {
    headers.set("Content-Type", "application/json");
  }
```

`api/kbs.ts`（钩子按既有 session/agents 模式）：`useDocuments` 的 `refetchInterval` 计算：

```ts
export function useDocuments(kbId: string | undefined) {
  return useQuery({
    queryKey: ["documents", kbId],
    queryFn: () => apiJson<DocumentItem[]>(`/api/v1/kbs/${kbId}/documents`),
    enabled: Boolean(kbId),
    refetchInterval: (query) => {
      const docs = query.state.data as DocumentItem[] | undefined;
      const active = docs?.some((d) => !["ready", "failed"].includes(d.status));
      return active ? 2000 : false;
    },
  });
}

export function useUploadDocument(kbId: string) {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (file: File) => {
      const form = new FormData();
      form.append("file", file);
      const res = await apiFetch(`/api/v1/kbs/${kbId}/documents`, { method: "POST", body: form });
      if (!res.ok) {
        const body = (await res.json().catch(() => ({}))) as { detail?: string };
        throw new Error(body.detail ?? `HTTP ${res.status}`);
      }
      return (await res.json()) as DocumentItem;
    },
    onSuccess: () => qc.invalidateQueries({ queryKey: ["documents", kbId] }),
  });
}
```

`KbPage/KbDetailPage/DocumentTable`：列表卡片 + 详情页（上传按钮/拖拽区、`DocumentTable`、返回）；`App.tsx` 与侧边栏入口照既有 agents 模式。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run: `pnpm run test -- --run`（期望 45 passed：42 + 3）；`pnpm build`

```powershell
git add frontend
git commit -m "feat(frontend): kb management pages with upload and status polling"
```

---

### Task 8: 智能体知识库绑定（后端端点 + 编辑器绑定区）

**Files:**
- Modify: `backend/app/schemas/agent.py`（`KbBindingIn`、`AgentOut.kb_bindings`）
- Modify: `backend/app/services/agent_service.py`（`set_kbs`、`list_kb_bindings`）
- Modify: `backend/app/api/v1/agents.py`（`PUT /agents/{aid}/kbs`；`_to_out` 填充 `kb_bindings`）
- Test: `backend/tests/test_agents_api.py`（+1）
- Frontend: `frontend/src/api/agents.ts`（`AgentItem.kb_bindings`、`useSetAgentKbs`）；`frontend/src/components/agents/KbBindings.tsx`；`frontend/src/pages/AgentEditorPage.tsx`（集成）；测试 +1

**Interfaces:**
- Produces: `PUT /agents/{aid}/kbs {bindings: [{kb_id, top_k?, score_threshold?}]}` → `{bindings: [...]}`（替换语义；kb 归属校验，非本人 404）；`AgentOut.kb_bindings: [{kb_id, name, top_k, score_threshold}]`。
- Produces: 前端绑定区（勾选 KB + top_k 数字，保存按钮）。

- [ ] **Step 1: 后端测试（追加 `test_agents_api.py`）**

```python
async def test_set_agent_kbs_and_kb_bindings_in_out(client, auth_headers):
    kb = (await client.post("/api/v1/kbs", json={"name": "KB1"}, headers=auth_headers)).json()
    aid = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()["id"]
    r = await client.put(
        f"/api/v1/agents/{aid}/kbs",
        json={"bindings": [{"kb_id": kb["id"], "top_k": 3}]},
        headers=auth_headers,
    )
    assert r.status_code == 200 and len(r.json()["bindings"]) == 1
    detail = (await client.get(f"/api/v1/agents/{aid}", headers=auth_headers)).json()
    assert detail["kb_bindings"][0]["kb_id"] == kb["id"]
    assert detail["kb_bindings"][0]["top_k"] == 3
    # 替换语义
    r2 = await client.put(f"/api/v1/agents/{aid}/kbs", json={"bindings": []}, headers=auth_headers)
    assert r2.json()["bindings"] == []

    # 他人 KB → 404
    from tests.test_sessions_api import make_user

    other = await make_user(client, "bob")
    r3 = await client.put(
        f"/api/v1/agents/{aid}/kbs",
        json={"bindings": [{"kb_id": kb["id"]}]},
        headers=other,
    )
    assert r3.status_code == 404
```

- [ ] **Step 2: 后端实现**

`agent_service.set_kbs(db, agent, user, bindings) -> list[AgentKB]`：对每个 binding 先校验 KB 归属（`select(KnowledgeBase).where(id, owner_id)`，非本人 → 404「知识库不存在」）；先 `delete(AgentKB).where(agent_id)` 再插入；提交。
`list_kb_bindings(db, agent_id)`：JOIN 名称返回 `[{kb_id, name, top_k, score_threshold}]`；`_to_out` 填充 `out.kb_bindings = await agent_service.list_kb_bindings(db, agent.id)`。
`schemas`：`KbBindingIn {kb_id: uuid.UUID, top_k: int = 5 (ge=1 le=20), score_threshold: float = 0.3 (ge=0 le=1)}`；`KbBindingOut`；`KbsIn {bindings: list[KbBindingIn]}`；`AgentOut.kb_bindings: list[dict] = []`。

- [ ] **Step 3: 前端绑定区 + 测试**

`KbBindings.tsx`：props `{agentId, bindings, editable}`；`useKbs()` 列 KB；勾选生成草稿 `{kb_id, top_k}`；top_k 数字输入；保存 → `useSetAgentKbs().mutateAsync({agentId, bindings})` + invalidate。测试（mock `@/api/kbs` 与 `@/api/agents`）：勾选 KB → 保存调用 mutation 且参数含 kb_id。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run（后端）: `uv run pytest -v`（期望 87 passed：86 + 1）；`uv run ruff check .`
Run（前端）: `pnpm run test -- --run`（期望 46 passed：45 + 1）；`pnpm build`

```powershell
git add backend frontend
git commit -m "feat: agent kb bindings with editor section"
```

---

### Task 9: 聊天引用 UI（角标/依据折叠/抽屉）+ 流式工具事件消费

**Files:**
- Modify: `frontend/src/stores/chatStream.ts`（`active` 增 `citations: Citation[]` 与 `toolEvents: ToolEvent[]`；新增 append 方法）
- Create: `frontend/src/lib/citations.ts`（`linkifyCitations(content, refs)`：把 `[n]` 替换为 `[[n]](#cite-n)` markdown 链接）+ `frontend/src/lib/__tests__/citations.test.ts`
- Create: `frontend/src/components/chat/CitationList.tsx`（「回答依据」折叠区：`[n] 文件名 pN`，点击回调）
- Modify: `frontend/src/components/chat/MarkdownContent.tsx`（渲染锚点 `href^="#cite-"` 时注入点击拦截与样式）
- Modify: `frontend/src/components/chat/MessageItem.tsx`（assistant 消息渲染 citation 块 → CitationList；`onOpenCitation` 透传）
- Modify: `frontend/src/components/chat/ChatView.tsx`（citation/tool_call/tool_result 事件流式消费；右侧抽屉展示 snippet；`linkifyCitations` 应用）
- Tests: `citations.test.ts`（1 条：`linkifyCitations("答案[1]", 2)` 含 `#cite-1`；越界 `[9]` 不转换）+ ChatView 测试 +1（citation 事件后显示依据与可点击）+ store 测试 +1（toolEvents 追加）

**Interfaces:**
- Produces: `Citation {ref, chunk_id, source, page, score, snippet}`；streaming 叠加层渲染顺序 `thinking → citations → toolEvents → text`；点击 `[n]` 或依据项 → ChatView 抽屉（`openCitation` state）。
- Consumes: 后端 `citation` 事件（T6）、`tool_call/tool_result` 事件（M2/T6）。

- [ ] **Step 1: 写失败测试**

`citations.test.ts`：

```ts
import { expect, test } from "vitest";
import { linkifyCitations } from "@/lib/citations";

test("只转换存在的引用编号", () => {
  const out = linkifyCitations("并发要点[1]，锁[2]，越界[9]", 2);
  expect(out).toContain("[1](#cite-1)");
  expect(out).toContain("[2](#cite-2)");
  expect(out).toContain("[9]");
  expect(out).not.toContain("#cite-9");
});
```

`chatStream.test.ts` 追加：

```ts
test("toolEvents 与 citations 追加", () => {
  const s = useChatStreamStore.getState();
  s.clear();
  s.start("m1", "s1");
  s.appendToolEvent({ type: "tool_call", id: "c1", tool: "time_now", args: {} });
  s.appendCitation({ ref: 1, chunk_id: "c", source: "a.md", page: 2, score: 0.5, snippet: "s" });
  const active = useChatStreamStore.getState().active!;
  expect(active.toolEvents).toHaveLength(1);
  expect(active.citations[0].ref).toBe(1);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `pnpm run test -- --run src/lib/__tests__/citations.test.ts src/stores/__tests__/chatStream.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

`citations.ts`：

```ts
export interface Citation {
  ref: number;
  chunk_id: string;
  source: string;
  page: number | null;
  score: number;
  snippet: string;
}

export function linkifyCitations(content: string, maxRef: number): string {
  return content.replace(/\[(\d+)\]/g, (whole, n: string) => {
    const ref = Number(n);
    return ref >= 1 && ref <= maxRef ? `[${n}](#cite-${n})` : whole;
  });
}
```

`chatStream.ts`：`active: {id, sessionId, content, thinking, citations: Citation[], toolEvents: ToolEvent[]}`；`appendCitation(c)`、`appendToolEvent(e)`；`start` 初始化为空数组。

`MarkdownContent.tsx`：增加可选 `onCitation?: (ref: number) => void`；components 覆盖 `a`：`href.startsWith("#cite-")` → `onClick={e => {e.preventDefault(); onCitation?.(Number(href.slice(6)))}}`。

`CitationList.tsx`：折叠 `<details>`（默认展开当只有一个/流式中？实现者按简洁处理）：每项 `[n] 来源 · pN（score）`，按钮点击 `onOpen(citation)`。

`ChatView.tsx`：
- `onStreamEvent` 追加分支：`citation` → `appendCitation(data)`；`tool_call/tool_result` → `appendToolEvent(data)`（替换 M2 的"等待 invalidate"行为，工具卡片实时出现）。
- 叠加消息 blocks 组装：`[...thinking, ...citations(转 block), ...toolEvents(转 block), text]`。
- 历史消息：`MessageItem` 从 blocks 提取 citation 块渲染 `CitationList`。
- 抽屉：`const [openCitation, setOpenCitation] = useState<Citation | null>(null)`；右侧 `aside` 显示 source/page/snippet。
- 文本渲染：`BlockRenderer` 的 text 分支传 `maxRef`（同消息 citation 数）与 `onCitation`（`MarkdownContent` 内 `linkifyCitations`）。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run: `pnpm run test -- --run`（期望 ≥49 passed）；`pnpm build`

```powershell
git add frontend
git commit -m "feat(frontend): citation ui and streaming tool events"
```

---

### Task 10: 演示知识库 seed + README + 端到端验收 + 收尾

**Files:**
- Create: `backend/scripts/seed_kb.py`
- Modify: `README.md`（M3 功能与演示数据）
- （控制器收尾：打标签、推送；实现者不打 tag、不 push）

**Interfaces:**
- Produces: demo 用户下知识库「Java 并发笔记」（含 1 个已就绪文档、若干 chunks），并绑定到「代码专家」智能体（top_k=3）；幂等（按 KB 名跳过）。

- [ ] **Step 1: 实现 seed 脚本（含真实 ingest，bge-m3 已就绪）**

```python
import asyncio
import uuid

from sqlalchemy import select

from app.ai.rag.pipeline import run_ingest
from app.core.config import settings
from app.core.db import SessionLocal
from app.models import Agent, AgentKB, Document, KnowledgeBase, User

SAMPLE = """# Java 并发笔记

## synchronized
synchronized 是可重入的内置锁，JDK 1.6 后包含偏向锁、轻量级锁到重量级锁的升级过程。

## volatile
volatile 保证可见性与禁止指令重排，但不保证原子性。

## 线程池
线程池核心参数包括 corePoolSize、maximumPoolSize、workQueue、RejectedExecutionHandler。
"""


async def main() -> None:
    async with SessionLocal() as db:
        user = await db.scalar(select(User).where(User.username == "demo"))
        if user is None:
            print("请先运行 scripts.seed 创建 demo 用户")
            return
        exists = await db.scalar(
            select(KnowledgeBase).where(
                KnowledgeBase.owner_id == user.id, KnowledgeBase.name == "Java 并发笔记"
            )
        )
        if exists is not None:
            print("演示知识库已存在，跳过")
            return
        kb = KnowledgeBase(owner_id=user.id, name="Java 并发笔记", description="M3 演示数据")
        db.add(kb)
        await db.flush()
        stored = f"{uuid.uuid4()}.md"
        (settings_upload_dir_path := __import__("pathlib").Path(settings.upload_dir)).mkdir(
            parents=True, exist_ok=True
        )
        (settings_upload_dir_path / stored).write_text(SAMPLE, encoding="utf-8")
        doc = Document(
            kb_id=kb.id,
            filename="java-concurrency.md",
            file_type="md",
            size_bytes=len(SAMPLE.encode()),
            uploaded_by=user.id,
            meta={"stored_name": stored},
        )
        db.add(doc)
        await db.commit()
        await run_ingest(str(doc.id))
        agent = await db.scalar(
            select(Agent).where(Agent.owner_id == user.id, Agent.name == "代码专家")
        )
        if agent is not None:
            db.add(AgentKB(agent_id=agent.id, kb_id=kb.id, top_k=3))
            await db.commit()
        print(f"已创建演示知识库 {kb.id}（文档 {doc.id}），并绑定到代码专家")


if __name__ == "__main__":
    asyncio.run(main())
```

> 实现者请规范 `settings_upload_dir_path` 的写法（`Path(settings.upload_dir)` 直接两行），保持逻辑不变。

- [ ] **Step 2: 运行并验证**

Run: `uv run python -m scripts.seed_kb`（期望创建成功并可重复运行跳过）
Run: `uv run pytest -v`（期望 ≥87 passed）；`uv run ruff check .`
Run（前端）: `pnpm run test -- --run`（期望 ≥49 passed）；`pnpm build`
抽查：`docker compose exec postgres psql -U webagent -d webagent -c "select count(*) from chunks;"`（≥3）

- [ ] **Step 3: README 更新**

功能列表追加：知识库（上传/解析/切片/嵌入流水线、状态徽章）、混合检索（pgvector+jieba FTS+RRF）、回答引用（角标/依据/抽屉）、智能体知识库绑定；演示数据小节追加 `uv run python -m scripts.seed_kb`。

- [ ] **Step 4: 端到端验收（控制器执行；实现者只保证上述脚本与回归）**

真实模型（经 Vite 代理）：
1. 登录 demo → `GET /kbs` 可见「Java 并发笔记」，文档 `ready`、chunk_count>0；
2. 用「代码专家」建会话（已绑定 KB）→ 问「线程池的核心参数有哪些？」→ SSE 出现 `event: citation`（`source=java-concurrency.md`）→ 回答含 `[1]` 且内容覆盖 corePoolSize 等；
3. `GET messages`：blocks 首块为 `citation`，text 含 `[1]`；
4. DB 抽查 `spans` 有 `retrieval` 记录（status=ok，output.chunks 非空）；
5. 上传新文件（可选）：上传 → 状态从 pending→ready；
6. 前端手测：`/kb` 详情页状态徽章/重试按钮；聊天「回答依据」展开、点击 `[1]` 打开抽屉。

- [ ] Step 5: 提交

```powershell
git add backend/scripts/seed_kb.py README.md
git commit -m "chore: demo knowledge base seed and m3 docs"
```

> 完成后由控制器打标签 `m3-rag` 并按既有流程推送/PR。

---

## 完成定义（M3 Done）

- [ ] 后端 `uv run pytest -v` ≥ 87 passed；`uv run ruff check .` 干净
- [ ] 前端 `pnpm run test -- --run` ≥ 49 passed；`pnpm build` 通过
- [ ] API 级验收（T10 Step 4）1~4 全过；前端手测 6 项全过
- [ ] `seed_kb` 幂等；README 更新；`arq` worker 可启动（`uv run arq app.workers.settings.WorkerSettings` 不报错）
- [ ] 标签 `m3-rag` 已打（控制器）；分支已推送

## 已知延后项（不在 M3 范围，记录备查）

- 扫描件 OCR（tesseract/paddleocr）；复杂表格解析；rerank 模型（bge-reranker，显存与调度 M4）
- `web_search`/`http_request` 工具（M4/M5）；引用在正文中的行内高亮样式精修（M6 打磨）
- 知识库评估脚本（黄金问答 recall@k，可砍项，M4 后再做）
- M2 遗留的流式工具卡片与 citation 的合并渲染次序细节（T9 已覆盖基本形态）
- 设计文档 02/05 与实现的字段同步（examples/tool_slugs/messages.seq、citation 事件字段名 message_id/ref）