# M2：智能体系统 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让「智能体」成为一等公民：可创建/配置/发布/回滚的智能体（系统提示词+变量插值、模型参数、工具集、欢迎语/标签/示例问题、版本快照），聊天时可选择智能体，运行时会**执行工具调用循环**（ReAct，`time_now` 可用、`kb_search` 占位）并把工具调用完整持久化与流式呈现。

**Architecture:** 数据库扩展 `agents` 表并新增 `agent_versions/tools/agent_tools`；`app/ai/tools/` 提供装饰器注册表（签名→JSON Schema）与内置工具，启动时同步到 `tools` 表；`app/ai/runtime.py` 升级为「按智能体解析配置（提示词插值+模型参数+工具）→ 多轮工具循环（≤5 轮）→ 类型化 SSE（新增 tool_call/tool_result）→ blocks 持久化」；前端新增 `/agents` 管理页（列表+编辑器+发布/版本/回滚）与会话侧「选择智能体新建任务」。

**Tech Stack:** 沿用 M0/M1（FastAPI/SQLAlchemy async/Alembic；React 19/Vite/TS/Tailwind/shadcn base-nova/TanStack Query）。前端新增组件：`pnpm dlx shadcn@latest add slider checkbox textarea badge`（复用 base-nova 组件体系）。后端零新依赖。

**Spec（设计文档）:** `docs/设计/02-数据库设计.md`（agents/agent_versions/tools/agent_tools）、`04-智能体管理模块.md`（model_config/工具注册/版本控制）、`06-会话与消息流.md`（ReAct 循环）、`09-前端交互设计.md`（智能体编辑器/消息操作）。

## Global Constraints

- 沿用 M0/M1 全部约束：API 前缀 `/api/v1`；Windows 11 + PowerShell；中文 UI 文案；提交前缀 `feat:/fix:/chore:/test:` 且每任务提交一次；测试输出 pristine；JWT/bcrypt/PG16+pgvector/Redis；`ensure_ascii=False`。
- **分支策略**：`feat/m2-agents` 从 `feat/m1-sessions` 尖端（`1f28558`）创建（堆叠；合并顺序 M0→M1→M2）。
- SSE 事件协议本期扩展两种：`tool_call {message_id, id, name, args}`、`tool_result {message_id, id, status, elapsed_ms, preview}`；其余沿用 M1 五种。
- `model_config` JSONB Schema（设计 04）：`{provider:"ollama", model, temperature(0~2), top_p(0~1), max_tokens(128~8192), num_ctx(2048~32768), supports_thinking, history_rounds(1~50), fallback_model:null}`；服务端 Pydantic 校验，越界 422。
- 提示词变量：`{{user.name}}`/`{{today}}`/`{{session.title}}`/`{{agent.name}}`；未知变量原样保留；保存接口返回 `variables` 列表（非阻断警告）。
- 版本语义：发布=把当前草稿打包快照写 `agent_versions`（version+1 并更新 `agents.current_version`）；回滚=把历史快照**拷回草稿**（不产生新版本）；消息记录生成时的 `agent_version`；被消息引用的智能体**禁止物理删除**（409，改用归档）。
- 工具循环：最多 5 轮；未知工具/执行异常 → `tool_result.status=error` 且不中断整体（把错误文本回填给模型）；工具执行串行。
- `messages` 新增 `seq BIGINT IDENTITY` 单调序号列；所有消息排序改用 `seq`（`created_at` 仅作展示）。
- 新增后端依赖：无；新增前端 npm 依赖：无（仅 shadcn 组件文件）。
- 所有命令在对应目录执行（后端 `D:\code\webAgent\backend`、前端 `D:\code\webAgent\frontend`、git 在仓库根）。

---

### Task 1: 数据模型扩展 + 迁移（agents 扩展 / agent_versions / tools / agent_tools / messages.seq）

**Files:**
- Modify: `backend/app/models/agent.py`（扩展 Agent，新增 AgentVersion）
- Create: `backend/app/models/tool.py`（Tool / AgentTool）
- Modify: `backend/app/models/session.py`（`Message.seq`）
- Modify: `backend/app/models/__init__.py`（导出新模型）
- Modify: `backend/app/services/session_service.py`、`backend/app/services/message_service.py`、`backend/app/ai/runtime.py`（排序改用 `seq`）
- Create: `backend/alembic/versions/<auto>_m2_agents_tools_versions_seq.py`
- Test: `backend/tests/test_m2_models.py`

**Interfaces:**
- Produces: `Agent`（全字段）、`AgentVersion`、`Tool`、`AgentTool`、`Message.seq`。
- Produces: 消息排序一律 `Message.seq`（asc）/ `desc`（历史加载）。

- [ ] **Step 1: 创建 M2 分支并写失败测试 `backend/tests/test_m2_models.py`**

```powershell
cd D:\code\webAgent
git switch -c feat/m2-agents
git add docs/superpowers/plans/2026-09-11-M2-智能体系统.md
git commit -m "docs: add m2 implementation plan"
```

```python
import uuid

from sqlalchemy import select

from app.core.security import hash_password
from app.models import Agent, AgentVersion, Message, Session, Tool, User


async def _user(db) -> User:
    u = User(
        id=uuid.uuid4(),
        username="alice",
        email="alice@example.com",
        password_hash=hash_password("Passw0rd!"),
    )
    db.add(u)
    await db.flush()
    return u


async def test_agent_full_fields_and_version(session_maker):
    async with session_maker() as db:
        u = await _user(db)
        agent = Agent(
            owner_id=u.id,
            name="代码专家",
            emoji="💻",
            description="review 代码",
            tags=["dev"],
            system_prompt="你是代码专家，今天是 {{today}}",
            model_config={"model": "qwen2.5:7b-instruct-q4_K_M", "temperature": 0.3},
            welcome_msg="贴代码给我",
            examples=["帮我 review 这段"],
        )
        db.add(agent)
        await db.flush()
        db.add(
            AgentVersion(
                agent_id=agent.id, version=1, snapshot={"name": "代码专家"}, published_by=u.id
            )
        )
        await db.commit()

        got = await db.scalar(select(Agent).where(Agent.id == agent.id))
        assert got.emoji == "💻" and got.tags == ["dev"] and got.examples == ["帮我 review 这段"]
        v = await db.scalar(select(AgentVersion).where(AgentVersion.agent_id == agent.id))
        assert v.version == 1 and v.snapshot["name"] == "代码专家"


async def test_tool_binding(session_maker):
    async with session_maker() as db:
        u = await _user(db)
        agent = Agent(owner_id=u.id, name="A", system_prompt="x", model_config={})
        db.add(agent)
        await db.flush()
        t = Tool(slug="time_now", name="当前时间", description="d", input_schema={}, category="system")
        db.add(t)
        await db.flush()
        from app.models import AgentTool

        db.add(AgentTool(agent_id=agent.id, tool_id=t.id, config={"timeout": 5}))
        await db.commit()
        got = await db.scalar(
            select(Tool).join(AgentTool, AgentTool.tool_id == Tool.id).where(AgentTool.agent_id == agent.id)
        )
        assert got.slug == "time_now"


async def test_messages_seq_strict_order(session_maker):
    """同一事务写入的两条消息 created_at 相同，但 seq 严格递增。"""
    async with session_maker() as db:
        u = await _user(db)
        s = Session(user_id=u.id)
        db.add(s)
        await db.flush()
        m1 = Message(session_id=s.id, role="user", blocks=[{"type": "text", "content": "一"}])
        m2 = Message(session_id=s.id, role="assistant", blocks=[{"type": "text", "content": "二"}])
        db.add_all([m1, m2])
        await db.commit()
        rows = (
            await db.scalars(select(Message).where(Message.session_id == s.id).order_by(Message.seq))
        ).all()
        assert [r.blocks[0]["content"] for r in rows] == ["一", "二"]
        assert rows[0].seq < rows[1].seq
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_m2_models.py -v`
Expected: FAIL（`cannot import name 'AgentVersion'` / `Message` 无 `seq`）

- [ ] **Step 3: 扩展模型**

`backend/app/models/agent.py` 全量替换：

```python
import uuid
from datetime import datetime

from sqlalchemy import DateTime, ForeignKey, Integer, String, Text, UniqueConstraint, func
from sqlalchemy.dialects.postgresql import ARRAY, JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import Base


class Agent(Base):
    __tablename__ = "agents"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    owner_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("users.id", ondelete="CASCADE"), index=True
    )
    name: Mapped[str] = mapped_column(String(64))
    emoji: Mapped[str] = mapped_column(String(8), default="🤖", server_default="🤖")
    description: Mapped[str | None] = mapped_column(Text)
    tags: Mapped[list[str]] = mapped_column(ARRAY(String), default=list, server_default="{}")
    system_prompt: Mapped[str] = mapped_column(Text, default="", server_default="")
    model_config: Mapped[dict] = mapped_column(JSONB, default=dict, server_default="{}")
    welcome_msg: Mapped[str | None] = mapped_column(Text)
    examples: Mapped[list] = mapped_column(JSONB, default=list, server_default="[]")
    status: Mapped[str] = mapped_column(String(16), default="draft", server_default="draft")
    current_version: Mapped[int] = mapped_column(Integer, default=1, server_default="1")
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True), server_default=func.now(), onupdate=func.now()
    )


class AgentVersion(Base):
    __tablename__ = "agent_versions"
    __table_args__ = (UniqueConstraint("agent_id", "version"),)

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    agent_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("agents.id", ondelete="CASCADE"), index=True
    )
    version: Mapped[int] = mapped_column(Integer)
    snapshot: Mapped[dict] = mapped_column(JSONB)
    published_by: Mapped[uuid.UUID | None] = mapped_column(ForeignKey("users.id"), nullable=True)
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), server_default=func.now())
```

`backend/app/models/tool.py`：

```python
import uuid

from sqlalchemy import Boolean, ForeignKey, String, Text
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.orm import Mapped, mapped_column

from app.models.base import Base


class Tool(Base):
    __tablename__ = "tools"

    id: Mapped[uuid.UUID] = mapped_column(primary_key=True, default=uuid.uuid4)
    slug: Mapped[str] = mapped_column(String(64), unique=True)
    name: Mapped[str] = mapped_column(String(64))
    description: Mapped[str] = mapped_column(Text)
    input_schema: Mapped[dict] = mapped_column(JSONB, default=dict)
    category: Mapped[str] = mapped_column(String(32), default="general")
    is_system: Mapped[bool] = mapped_column(Boolean, default=False)
    enabled: Mapped[bool] = mapped_column(Boolean, default=True)


class AgentTool(Base):
    __tablename__ = "agent_tools"

    agent_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("agents.id", ondelete="CASCADE"), primary_key=True
    )
    tool_id: Mapped[uuid.UUID] = mapped_column(
        ForeignKey("tools.id", ondelete="CASCADE"), primary_key=True
    )
    config: Mapped[dict] = mapped_column(JSONB, default=dict)
```

`backend/app/models/session.py` 的 `Message` 增加（并导入 `BigInteger, Identity`）：

```python
    seq: Mapped[int] = mapped_column(BigInteger, Identity(), nullable=False, index=True)
```

`backend/app/models/__init__.py` 追加导出 `AgentVersion`、`Tool`、`AgentTool`。

- [ ] **Step 4: 排序改用 seq**

- `session_service.list_messages`：`.order_by(Message.seq)`
- `message_service.truncate_session`：`.order_by(Message.seq)`
- `runtime._load_history`：`.order_by(Message.seq.desc())`
- `tests/conftest.py` 的 `clean_tables` 扩展为：

```python
        await c.execute(
            text(
                "TRUNCATE attachments, messages, sessions, refresh_tokens, users, agents,"
                " agent_versions, agent_tools, tools CASCADE"
            )
        )
```

- [ ] **Step 5: 生成并应用迁移（autogenerate + 手工补 seq）**

Run: `uv run alembic revision --autogenerate -m "m2 agents tools versions messages seq"`
手工在该迁移的 `upgrade()` 内、新表创建之后补充（autogenerate 可能未识别 Identity 列，若已包含则跳过）：

```python
    op.add_column("messages", sa.Column("seq", sa.BigInteger(), sa.Identity(), nullable=False))
    op.create_index("ix_messages_seq", "messages", ["seq"], unique=False)
```

`downgrade()` 逆序补充：

```python
    op.drop_index("ix_messages_seq", table_name="messages")
    op.drop_column("messages", "seq")
```

Run: `uv run alembic upgrade head`；`uv run alembic check`（零漂移）；`docker compose exec postgres psql -U webagent -d webagent -c "\d messages"` 确认 `seq bigint GENERATED ... IDENTITY`
Expected: 迁移应用成功；既有 1000+ 消息自动回填 seq。

- [ ] **Step 6: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 45 passed：42 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): m2 data model - agents, tools, versions, messages seq"
```

---

### Task 2: 工具注册表 + 内置工具（time_now / kb_search 占位）+ 启动同步 + GET /tools

**Files:**
- Create: `backend/app/ai/tools/__init__.py`、`backend/app/ai/tools/registry.py`、`backend/app/ai/tools/builtins.py`
- Modify: `backend/app/main.py`（lifespan 内同步工具表）
- Create: `backend/app/api/v1/tools.py`；Modify: `backend/app/api/v1/router.py`
- Test: `backend/tests/test_tools_registry.py`

**Interfaces:**
- Produces: `@tool(slug, name, description, category="general", is_system=False)` 装饰器；`TOOL_REGISTRY: dict[str, ToolDef]`；`schema_from_signature(fn) -> dict`；`get_tool(slug) -> ToolDef | None`；`async sync_tools(db) -> None`（按 slug upsert）；`tools_payload(slugs: list[str]) -> list[dict]`（Ollama function 定义）。
- Produces: `GET /api/v1/tools` → `[{id, slug, name, description, category, is_system}]`（读 DB，未同步时为空数组）。
- 内置：`time_now`（本地日期+星期）、`kb_search`（M3 前返回占位文案）。

- [ ] **Step 1: 写失败测试 `backend/tests/test_tools_registry.py`**

```python
from datetime import datetime

from sqlalchemy import select

from app.ai.tools import builtins  # noqa: F401  触发注册
from app.ai.tools.registry import TOOL_REGISTRY, get_tool, schema_from_signature, sync_tools, tools_payload
from app.models import Tool


async def test_schema_from_signature():
    async def sample(query: str, top_k: int = 5) -> str:
        return ""

    schema = schema_from_signature(sample)
    assert schema["properties"]["query"]["type"] == "string"
    assert schema["properties"]["top_k"]["default"] == 5


async def test_registry_has_builtins():
    assert "time_now" in TOOL_REGISTRY and "kb_search" in TOOL_REGISTRY
    payload = tools_payload(["time_now"])
    assert payload[0]["function"]["name"] == "time_now"
    assert payload[0]["type"] == "function"


async def test_time_now_executes():
    result = await get_tool("time_now").handler()
    assert str(datetime.now().year) in result


async def test_kb_search_is_placeholder():
    result = await get_tool("kb_search").handler("任意查询")
    assert "知识库" in result


async def test_sync_tools_upserts(session_maker):
    async with session_maker() as db:
        await sync_tools(db)
        rows = (await db.scalars(select(Tool))).all()
        assert {r.slug for r in rows} >= {"time_now", "kb_search"}
        await sync_tools(db)  # 幂等
        rows2 = (await db.scalars(select(Tool).where(Tool.slug == "time_now"))).all()
        assert len(rows2) == 1
```

- [ ] **Step 2: 运行确认失败（模块不存在）**

Run: `uv run pytest tests/test_tools_registry.py -v`
Expected: FAIL（ModuleNotFoundError）

- [ ] **Step 3: 实现注册表与内置工具**

`backend/app/ai/tools/registry.py`：

```python
import inspect
from collections.abc import Callable
from dataclasses import dataclass
from typing import Any

from pydantic import create_model
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models import Tool


@dataclass
class ToolDef:
    slug: str
    name: str
    description: str
    input_schema: dict
    handler: Callable[..., Any]
    category: str = "general"
    is_system: bool = False


TOOL_REGISTRY: dict[str, ToolDef] = {}


def schema_from_signature(fn: Callable) -> dict:
    params = inspect.signature(fn).parameters
    fields = {
        name: (
            p.annotation if p.annotation is not inspect.Parameter.empty else str,
            p.default if p.default is not inspect.Parameter.empty else ...,
        )
        for name, p in params.items()
    }
    return create_model(fn.__name__, **fields).model_json_schema()


def tool(slug: str, name: str, description: str, category: str = "general", is_system: bool = False):
    def decorator(fn: Callable) -> Callable:
        TOOL_REGISTRY[slug] = ToolDef(
            slug=slug,
            name=name,
            description=description,
            input_schema=schema_from_signature(fn),
            handler=fn,
            category=category,
            is_system=is_system,
        )
        return fn

    return decorator


def get_tool(slug: str) -> ToolDef | None:
    return TOOL_REGISTRY.get(slug)


def tools_payload(slugs: list[str]) -> list[dict]:
    payload = []
    for slug in slugs:
        item = TOOL_REGISTRY.get(slug)
        if item is not None:
            payload.append(
                {
                    "type": "function",
                    "function": {
                        "name": item.slug,
                        "description": item.description,
                        "parameters": item.input_schema,
                    },
                }
            )
    return payload


async def sync_tools(db: AsyncSession) -> None:
    for item in TOOL_REGISTRY.values():
        existing = await db.scalar(select(Tool).where(Tool.slug == item.slug))
        if existing is None:
            db.add(
                Tool(
                    slug=item.slug,
                    name=item.name,
                    description=item.description,
                    input_schema=item.input_schema,
                    category=item.category,
                    is_system=item.is_system,
                )
            )
        else:
            existing.name = item.name
            existing.description = item.description
            existing.input_schema = item.input_schema
            existing.category = item.category
            existing.is_system = item.is_system
    await db.commit()
```

`backend/app/ai/tools/builtins.py`：

```python
from datetime import datetime

from app.ai.tools.registry import tool


@tool(
    "time_now",
    "当前时间",
    "获取今天的日期与星期（服务器本地时区）",
    category="system",
    is_system=True,
)
async def time_now() -> str:
    now = datetime.now()
    weekdays = ["星期一", "星期二", "星期三", "星期四", "星期五", "星期六", "星期日"]
    return f"{now:%Y-%m-%d} {weekdays[now.weekday()]}"


@tool(
    "kb_search",
    "知识库检索",
    "在智能体绑定的知识库中检索相关文档片段（知识库将在 M3 提供）",
    category="knowledge",
    is_system=True,
)
async def kb_search(query: str, top_k: int = 5) -> str:
    return "知识库功能尚未启用（将在 M3 提供），请直接基于已有知识回答。"
```

`backend/app/ai/tools/__init__.py`：

```python
from app.ai.tools import builtins  # noqa: F401  导入即注册内置工具
from app.ai.tools.registry import TOOL_REGISTRY, get_tool, sync_tools, tool, tools_payload

__all__ = ["TOOL_REGISTRY", "get_tool", "sync_tools", "tool", "tools_payload"]
```

`backend/app/main.py` 的 lifespan 内 `app.state.provider = ...` 之后追加：

```python
    from app.ai.tools import sync_tools

    async with SessionLocal() as db:
        await sync_tools(db)
```

（`from app.core.db import SessionLocal` 补导入。）

- [ ] **Step 4: 实现 `GET /tools`**

`backend/app/api/v1/tools.py`：

```python
from fastapi import APIRouter, Depends
from pydantic import BaseModel, ConfigDict
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.v1.deps import get_current_user
from app.core.db import get_db
from app.models import Tool
from app.models.user import User

router = APIRouter(prefix="/tools", tags=["tools"])


class ToolOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: str
    slug: str
    name: str
    description: str
    category: str
    is_system: bool


@router.get("", response_model=list[ToolOut])
async def list_tools(
    user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    rows = (await db.scalars(select(Tool).where(Tool.enabled.is_(True)).order_by(Tool.slug))).all()
    return [
        ToolOut(
            id=str(r.id),
            slug=r.slug,
            name=r.name,
            description=r.description,
            category=r.category,
            is_system=r.is_system,
        )
        for r in rows
    ]
```

> 注：`ToolOut.id` 用 `str` 序列化（避免 pydantic UUID 直接暴露差异）；实现者可改为 `uuid.UUID` 并保持一致。

`router.py` 挂载 tools 路由。

- [ ] **Step 5: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 50 passed：45 + 5）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): tool registry with builtins and sync"
```

---

### Task 3: 智能体 CRUD API + 提示词变量提取 + GET /models

**Files:**
- Create: `backend/app/schemas/agent.py`、`backend/app/services/agent_service.py`、`backend/app/api/v1/agents.py`、`backend/app/api/v1/models.py`
- Modify: `backend/app/api/v1/router.py`
- Test: `backend/tests/test_agents_api.py`

**Interfaces:**
- Produces: `ModelConfig`（Pydantic，含范围校验）；`extract_variables(prompt: str) -> list[str]`；`render_prompt(template, resolvers) -> str`（runtime 在 T5 消费）。
- Produces: `POST/GET /agents`、`GET/PATCH/DELETE /agents/{id}`（DELETE 被引用时 409）；`GET /models` → `[{name, size_mb}]`（provider.list_available + `settings.default_model` 兜底）。
- AgentOut 字段：`id/name/emoji/description/tags/system_prompt/model_config/welcome_msg/examples/status/current_version/variables/created_at/updated_at`。

- [ ] **Step 1: 写失败测试 `backend/tests/test_agents_api.py`**

```python
import uuid

from app.ai.deps import get_provider
from app.main import app
from tests.fake_provider import FakeProvider

AGENT = {
    "name": "代码专家",
    "emoji": "💻",
    "description": "review 代码",
    "tags": ["dev"],
    "system_prompt": "你是{{agent.name}}，今天是 {{today}}，用户 {{user.name}}",
    "model_config": {"model": "qwen2.5:7b-instruct-q4_K_M", "temperature": 0.3},
    "welcome_msg": "贴代码给我",
    "examples": ["帮我 review 这段"],
}


async def test_create_agent_and_variables(client, auth_headers):
    r = await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)
    assert r.status_code == 201
    data = r.json()
    assert data["name"] == "代码专家" and data["status"] == "draft"
    assert set(data["variables"]) == {"agent.name", "today", "user.name"}


async def test_model_config_validation(client, auth_headers):
    bad = {**AGENT, "model_config": {"model": "m", "temperature": 2.5}}
    r = await client.post("/api/v1/agents", json=bad, headers=auth_headers)
    assert r.status_code == 422


async def test_list_update_archive_delete(client, auth_headers):
    aid = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()["id"]
    lst = await client.get("/api/v1/agents", headers=auth_headers)
    assert [a["id"] for a in lst.json()] == [aid]

    r = await client.patch(
        f"/api/v1/agents/{aid}", json={"name": "改名", "status": "archived"}, headers=auth_headers
    )
    assert r.json()["name"] == "改名" and r.json()["status"] == "archived"

    d = await client.delete(f"/api/v1/agents/{aid}", headers=auth_headers)
    assert d.status_code == 204


async def test_themes_no_cross_user(client, auth_headers):
    from tests.test_sessions_api import make_user

    aid = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()["id"]
    other = await make_user(client, "bob")
    assert (await client.get(f"/api/v1/agents/{aid}", headers=other)).status_code == 404
    assert (await client.patch(f"/api/v1/agents/{aid}", json={}, headers=other)).status_code == 404


async def test_models_endpoint(client, auth_headers):
    class P(FakeProvider):
        async def list_available(self):
            from app.ai.providers.base import ModelInfo

            return [ModelInfo(name="qwen2.5:7b-instruct-q4_K_M", size_mb=4700.0)]

    app.dependency_overrides[get_provider] = lambda: P()
    r = await client.get("/api/v1/models", headers=auth_headers)
    assert r.status_code == 200
    names = [m["name"] for m in r.json()]
    assert "qwen2.5:7b-instruct-q4_K_M" in names
```

- [ ] **Step 2: 运行确认失败（404/模块缺失）**

Run: `uv run pytest tests/test_agents_api.py -v`
Expected: FAIL

- [ ] **Step 3: 实现 schemas / service / routers**

`backend/app/schemas/agent.py`：

```python
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field


class ModelConfig(BaseModel):
    provider: str = "ollama"
    model: str
    temperature: float = Field(0.7, ge=0, le=2)
    top_p: float = Field(0.9, ge=0, le=1)
    max_tokens: int = Field(2048, ge=128, le=8192)
    num_ctx: int = Field(8192, ge=2048, le=32768)
    supports_thinking: bool = False
    history_rounds: int = Field(10, ge=1, le=50)
    fallback_model: str | None = None


class AgentCreateIn(BaseModel):
    name: str = Field(min_length=1, max_length=64)
    emoji: str = Field(default="🤖", max_length=8)
    description: str | None = None
    tags: list[str] = Field(default_factory=list, max_length=5)
    system_prompt: str = ""
    model_config: ModelConfig
    welcome_msg: str | None = None
    examples: list[str] = Field(default_factory=list, max_length=5)


class AgentUpdateIn(BaseModel):
    name: str | None = Field(default=None, min_length=1, max_length=64)
    emoji: str | None = Field(default=None, max_length=8)
    description: str | None = None
    tags: list[str] | None = Field(default=None, max_length=5)
    system_prompt: str | None = None
    model_config: ModelConfig | None = None
    welcome_msg: str | None = None
    examples: list[str] | None = Field(default=None, max_length=5)
    status: str | None = None


class AgentOut(BaseModel):
    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    name: str
    emoji: str
    description: str | None
    tags: list[str]
    system_prompt: str
    model_config: dict
    welcome_msg: str | None
    examples: list[str]
    status: str
    current_version: int
    variables: list[str] = []
    created_at: datetime
    updated_at: datetime
```

`backend/app/services/agent_service.py`：

```python
import re
import uuid

from fastapi import HTTPException
from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.models import Agent, Message
from app.models.user import User

VAR_RE = re.compile(r"\{\{\s*([\w.]+)\s*\}\}")


def extract_variables(prompt: str) -> list[str]:
    return sorted(set(VAR_RE.findall(prompt or "")))


async def get_owned_agent(db: AsyncSession, user: User, agent_id: uuid.UUID) -> Agent:
    agent = await db.scalar(
        select(Agent).where(Agent.id == agent_id, Agent.owner_id == user.id)
    )
    if agent is None:
        raise HTTPException(status_code=404, detail="智能体不存在")
    return agent


async def list_agents(db: AsyncSession, user: User) -> list[Agent]:
    rows = (
        await db.scalars(
            select(Agent)
            .where(Agent.owner_id == user.id)
            .order_by(Agent.updated_at.desc(), Agent.created_at.desc())
        )
    ).all()
    return list(rows)


async def create_agent(db: AsyncSession, user: User, data) -> Agent:
    agent = Agent(
        owner_id=user.id,
        name=data.name,
        emoji=data.emoji,
        description=data.description,
        tags=data.tags,
        system_prompt=data.system_prompt,
        model_config=data.model_config.model_dump(),
        welcome_msg=data.welcome_msg,
        examples=data.examples,
    )
    db.add(agent)
    await db.commit()
    await db.refresh(agent)
    return agent


async def update_agent(db: AsyncSession, agent: Agent, fields: dict) -> Agent:
    for key, value in fields.items():
        if value is None:
            continue
        if key == "model_config":
            value = value.model_dump() if hasattr(value, "model_dump") else value
        setattr(agent, key, value)
    await db.commit()
    await db.refresh(agent)
    return agent


async def delete_agent(db: AsyncSession, agent: Agent) -> None:
    used = await db.scalar(select(Message.id).where(Message.agent_id == agent.id).limit(1))
    if used is not None:
        raise HTTPException(status_code=409, detail="该智能体已被会话使用，请改为归档")
    await db.delete(agent)
    await db.commit()
```

`backend/app/api/v1/agents.py`：

```python
import uuid

from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.v1.deps import get_current_user
from app.core.db import get_db
from app.models.user import User
from app.schemas.agent import AgentCreateIn, AgentOut, AgentUpdateIn
from app.services import agent_service

router = APIRouter(prefix="/agents", tags=["agents"])


def _to_out(agent) -> AgentOut:
    out = AgentOut.model_validate(agent)
    out.variables = agent_service.extract_variables(agent.system_prompt)
    return out


@router.post("", response_model=AgentOut, status_code=201)
async def create_agent(
    body: AgentCreateIn,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    return _to_out(await agent_service.create_agent(db, user, body))


@router.get("", response_model=list[AgentOut])
async def list_agents(
    user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    return [_to_out(a) for a in await agent_service.list_agents(db, user)]


@router.get("/{aid}", response_model=AgentOut)
async def get_agent(
    aid: uuid.UUID, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    return _to_out(await agent_service.get_owned_agent(db, user, aid))


@router.patch("/{aid}", response_model=AgentOut)
async def patch_agent(
    aid: uuid.UUID,
    body: AgentUpdateIn,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    return _to_out(await agent_service.update_agent(db, agent, body.model_dump(exclude_unset=True)))


@router.delete("/{aid}", status_code=204)
async def delete_agent(
    aid: uuid.UUID, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    await agent_service.delete_agent(db, agent)
```

`backend/app/api/v1/models.py`：

```python
from fastapi import APIRouter, Depends

from app.ai.deps import get_provider
from app.ai.providers.base import ModelProvider
from app.api.v1.deps import get_current_user
from app.core.config import settings
from app.models.user import User

router = APIRouter(prefix="/models", tags=["models"])


@router.get("")
async def list_models(
    user: User = Depends(get_current_user), provider: ModelProvider = Depends(get_provider)
):
    models = await provider.list_available()
    names = {m.name for m in models}
    items = [{"name": m.name, "size_mb": m.size_mb} for m in models]
    if settings.default_model not in names:
        items.insert(0, {"name": settings.default_model, "size_mb": None})
    return items
```

`router.py` 挂载 agents 与 models。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 55 passed：50 + 5）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): agent CRUD with prompt variables and models endpoint"
```

---

### Task 4: 版本控制（发布快照 / 版本列表 / 回滚）+ 删除保护测试

**Files:**
- Modify: `backend/app/services/agent_service.py`（publish/list_versions/rollback/工具绑定读取）
- Modify: `backend/app/schemas/agent.py`（`AgentVersionOut`、`RollbackIn`）
- Modify: `backend/app/api/v1/agents.py`（三个新端点）
- Test: `backend/tests/test_agent_versions.py`

**Interfaces:**
- Produces: `publish_agent(db, agent, user) -> AgentVersion`（快照见下；version = max(已有)+1，首次为 1；`agent.current_version` 同步、`status`→`published`）
- Produces: `list_versions(db, agent) -> list[AgentVersion]`（desc）；`rollback_agent(db, agent, version) -> Agent`（快照拷回草稿 + 重绑工具，不产生新版本）
- Produces: `POST /agents/{id}/publish`、`GET /agents/{id}/versions`、`POST /agents/{id}/rollback {version}`
- 快照结构：`{name, emoji, description, tags, system_prompt, model_config, welcome_msg, examples, tool_slugs: [...]}`

- [ ] **Step 1: 写失败测试 `backend/tests/test_agent_versions.py`**

```python
import uuid

from sqlalchemy import select

from app.models import Agent, AgentTool, Message, Session, Tool, User
from tests.test_sessions_api import make_user

AGENT = {
    "name": "A",
    "system_prompt": "你是 {{agent.name}}",
    "model_config": {"model": "m1"},
    "tags": [],
    "examples": [],
}


async def _agent(client, auth_headers) -> dict:
    return (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()


async def test_publish_creates_incrementing_versions(client, auth_headers):
    aid = (await _agent(client, auth_headers))["id"]
    v1 = await client.post(f"/api/v1/agents/{aid}/publish", headers=auth_headers)
    assert v1.status_code == 200 and v1.json()["version"] == 1 and v1.json()["status"] == "published"

    await client.patch(f"/api/v1/agents/{aid}", json={"name": "A2"}, headers=auth_headers)
    v2 = await client.post(f"/api/v1/agents/{aid}/publish", headers=auth_headers)
    assert v2.json()["version"] == 2

    versions = await client.get(f"/api/v1/agents/{aid}/versions", headers=auth_headers)
    assert [v["version"] for v in versions.json()] == [2, 1]
    assert versions.json()[1]["snapshot"]["name"] == "A"


async def test_rollback_restores_snapshot_and_tools(client, auth_headers, session_maker):
    from app.ai.tools import sync_tools

    async with session_maker() as db:
        await sync_tools(db)
    aid = (await _agent(client, auth_headers))["id"]
    # 发布 v1（无工具），改名并绑定 time_now 后发布 v2
    await client.post(f"/api/v1/agents/{aid}/publish", headers=auth_headers)
    await client.patch(f"/api/v1/agents/{aid}", json={"name": "A2"}, headers=auth_headers)
    async with session_maker() as db:
        tool = await db.scalar(select(Tool).where(Tool.slug == "time_now"))
        assert tool is not None
        db.add(AgentTool(agent_id=uuid.UUID(aid), tool_id=tool.id))
        await db.commit()
    await client.post(f"/api/v1/agents/{aid}/publish", headers=auth_headers)

    # 改名后再回滚到 v1
    await client.patch(f"/api/v1/agents/{aid}", json={"name": "A3"}, headers=auth_headers)
    r = await client.post(
        f"/api/v1/agents/{aid}/rollback", json={"version": 1}, headers=auth_headers
    )
    assert r.status_code == 200 and r.json()["name"] == "A"
    async with session_maker() as db:
        rows = (await db.scalars(select(AgentTool).where(AgentTool.agent_id == uuid.UUID(aid)))).all()
        assert rows == []  # v1 无工具


async def test_delete_referenced_agent_conflicts(client, auth_headers, session_maker):
    aid = (await _agent(client, auth_headers))["id"]
    async with session_maker() as db:
        user = (await db.scalars(select(User))).first()
        s = Session(user_id=user.id, agent_id=uuid.UUID(aid))
        db.add(s)
        await db.flush()
        db.add(Message(session_id=s.id, role="user", blocks=[{"type": "text", "content": "x"}], agent_id=uuid.UUID(aid)))
        await db.commit()
    r = await client.delete(f"/api/v1/agents/{aid}", headers=auth_headers)
    assert r.status_code == 409
    # 归档可行
    ok = await client.patch(f"/api/v1/agents/{aid}", json={"status": "archived"}, headers=auth_headers)
    assert ok.status_code == 200
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_agent_versions.py -v`
Expected: FAIL（404 publish/versions/rollback）

- [ ] **Step 3: 实现**

`backend/app/services/agent_service.py` 追加：

```python
from sqlalchemy import delete, func

from app.models import AgentTool, AgentVersion, Tool


async def _tool_slugs(db: AsyncSession, agent_id) -> list[str]:
    rows = (
        await db.scalars(
            select(Tool.slug)
            .join(AgentTool, AgentTool.tool_id == Tool.id)
            .where(AgentTool.agent_id == agent_id, Tool.enabled.is_(True))
        )
    ).all()
    return list(rows)


def _snapshot(agent: Agent, tool_slugs: list[str]) -> dict:
    return {
        "name": agent.name,
        "emoji": agent.emoji,
        "description": agent.description,
        "tags": agent.tags,
        "system_prompt": agent.system_prompt,
        "model_config": agent.model_config,
        "welcome_msg": agent.welcome_msg,
        "examples": agent.examples,
        "tool_slugs": tool_slugs,
    }


async def publish_agent(db: AsyncSession, agent: Agent, user: User) -> AgentVersion:
    slugs = await _tool_slugs(db, agent.id)
    max_version = await db.scalar(
        select(func.max(AgentVersion.version)).where(AgentVersion.agent_id == agent.id)
    )
    version = int(max_version or 0) + 1
    row = AgentVersion(
        agent_id=agent.id, version=version, snapshot=_snapshot(agent, slugs), published_by=user.id
    )
    agent.current_version = version
    agent.status = "published"
    db.add(row)
    await db.commit()
    await db.refresh(row)
    await db.refresh(agent)
    return row


async def list_versions(db: AsyncSession, agent: Agent) -> list[AgentVersion]:
    rows = (
        await db.scalars(
            select(AgentVersion)
            .where(AgentVersion.agent_id == agent.id)
            .order_by(AgentVersion.version.desc())
        )
    ).all()
    return list(rows)


async def rollback_agent(db: AsyncSession, agent: Agent, version: int) -> Agent:
    row = await db.scalar(
        select(AgentVersion).where(
            AgentVersion.agent_id == agent.id, AgentVersion.version == version
        )
    )
    if row is None:
        raise HTTPException(status_code=404, detail="版本不存在")
    snap = row.snapshot
    for key in (
        "name",
        "emoji",
        "description",
        "tags",
        "system_prompt",
        "model_config",
        "welcome_msg",
        "examples",
    ):
        setattr(agent, key, snap.get(key))
    await db.execute(delete(AgentTool).where(AgentTool.agent_id == agent.id))
    for slug in snap.get("tool_slugs", []):
        tool = await db.scalar(select(Tool).where(Tool.slug == slug, Tool.enabled.is_(True)))
        if tool is not None:
            db.add(AgentTool(agent_id=agent.id, tool_id=tool.id))
    await db.commit()
    await db.refresh(agent)
    return agent
```

`backend/app/schemas/agent.py` 追加：

```python
class AgentVersionOut(BaseModel):
    version: int
    snapshot: dict
    created_at: datetime

    model_config = ConfigDict(from_attributes=True)


class PublishOut(BaseModel):
    version: int
    status: str


class RollbackIn(BaseModel):
    version: int
```

`backend/app/api/v1/agents.py` 追加：

```python
@router.post("/{aid}/publish", response_model=PublishOut)
async def publish_agent(
    aid: uuid.UUID, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    row = await agent_service.publish_agent(db, agent, user)
    return PublishOut(version=row.version, status=agent.status)


@router.get("/{aid}/versions", response_model=list[AgentVersionOut])
async def list_versions(
    aid: uuid.UUID, user: User = Depends(get_current_user), db: AsyncSession = Depends(get_db)
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    return await agent_service.list_versions(db, agent)


@router.post("/{aid}/rollback", response_model=AgentOut)
async def rollback_agent(
    aid: uuid.UUID,
    body: RollbackIn,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    return _to_out(await agent_service.rollback_agent(db, agent, body.version))
```

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 58 passed：55 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): agent version publish and rollback"
```

---

### Task 5: 运行时接入智能体（提示词插值 / 模型参数 / 会话绑定 / agent_version）

**Files:**
- Create: `backend/app/ai/agent_config.py`
- Modify: `backend/app/ai/runtime.py`（使用 EffectiveConfig）
- Modify: `backend/app/schemas/session.py`（`SessionCreateIn.agent_id`、`SessionOut.agent_id`）
- Modify: `backend/app/services/session_service.py`（create 校验 agent）；`backend/app/api/v1/sessions.py`（透传）
- Test: `backend/tests/test_agent_runtime.py`

**Interfaces:**
- Produces: `EffectiveConfig`（dataclass：agent_id/agent_version/model/temperature/top_p/max_tokens/num_ctx/history_rounds/system_prompt/tool_slugs）
- Produces: `resolve_effective_config(db, session) -> EffectiveConfig`（无 agent → settings 默认值）
- Produces: `render_prompt(template, *, user_name, session_title, agent_name) -> str`（`VAR_RESOLVERS` 四变量）
- 会话创建：`agent_id` 可空；提供时必须属于当前用户且 `status != "archived"`（否则 404/422）；`SessionOut.agent_id` 透出。

- [ ] **Step 1: 写失败测试 `backend/tests/test_agent_runtime.py`**

```python
from app.ai.deps import get_provider
from app.main import app
from tests.fake_provider import FakeProvider

AGENT = {
    "name": "辅导员",
    "system_prompt": "你是{{agent.name}}，今天是 {{today}}",
    "model_config": {"model": "qwen2.5:7b-instruct-q4_K_M", "temperature": 0.3, "history_rounds": 3},
    "tags": [],
    "examples": [],
}


async def test_session_with_agent_uses_config(client, auth_headers):
    agent = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()
    s = (
        await client.post(
            "/api/v1/sessions", json={"agent_id": agent["id"]}, headers=auth_headers
        )
    ).json()
    assert s["agent_id"] == agent["id"]

    provider = FakeProvider([("token", {"delta": "好"})])
    app.dependency_overrides[get_provider] = lambda: provider
    # 关闭标题任务干扰
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    r = await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    assert "event: done" in r.text

    req = provider.last_request
    assert req.model == "qwen2.5:7b-instruct-q4_K_M"
    assert req.temperature == 0.3
    assert req.messages[0]["role"] == "system"
    assert "辅导员" in req.messages[0]["content"]

    msgs = (await client.get(f"/api/v1/sessions/{s['id']}/messages", headers=auth_headers)).json()
    assert msgs[1]["agent_id"] == agent["id"]
    assert msgs[1]["agent_version"] == 1


async def test_session_without_agent_has_no_system_message(client, auth_headers):
    s = (await client.post("/api/v1/sessions", json={}, headers=auth_headers)).json()
    provider = FakeProvider([("token", {"delta": "x"})])
    app.dependency_overrides[get_provider] = lambda: provider
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    assert provider.last_request.messages[0]["role"] == "user"


async def test_session_agent_validation(client, auth_headers):
    from tests.test_sessions_api import make_user

    agent = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()
    other = await make_user(client, "bob")
    r = await client.post("/api/v1/sessions", json={"agent_id": agent["id"]}, headers=other)
    assert r.status_code == 404

    await client.patch(
        f"/api/v1/agents/{agent['id']}", json={"status": "archived"}, headers=auth_headers
    )
    r2 = await client.post("/api/v1/sessions", json={"agent_id": agent["id"]}, headers=auth_headers)
    assert r2.status_code == 422
```

> `MessageOut` 需补充 `agent_id` / `agent_version` 字段（schemas/session.py）。

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_agent_runtime.py -v`
Expected: FAIL（无 agent_id 字段/422）

- [ ] **Step 3: 实现**

`backend/app/ai/agent_config.py`：

```python
import re
import uuid
from dataclasses import dataclass, field

from sqlalchemy import select
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.config import settings
from app.models import Agent, AgentTool, Session, Tool

VAR_RE = re.compile(r"\{\{\s*([\w.]+)\s*\}\}")


def render_prompt(template: str, *, user_name: str, session_title: str, agent_name: str) -> str:
    today = __import__("datetime").date.today().isoformat()
    resolvers = {
        "user.name": user_name,
        "session.title": session_title,
        "agent.name": agent_name,
        "today": today,
    }

    def sub(m: re.Match) -> str:
        return resolvers.get(m.group(1), m.group(0))

    return VAR_RE.sub(sub, template or "")


@dataclass
class EffectiveConfig:
    agent_id: uuid.UUID | None = None
    agent_version: int | None = None
    model: str = field(default_factory=lambda: settings.default_model)
    temperature: float = 0.7
    top_p: float = 0.9
    max_tokens: int = 2048
    num_ctx: int = 8192
    history_rounds: int = field(default_factory=lambda: settings.history_rounds)
    system_prompt: str = ""
    tool_slugs: list[str] = field(default_factory=list)


async def resolve_effective_config(
    db: AsyncSession, session: Session, *, user_name: str
) -> EffectiveConfig:
    if session.agent_id is None:
        return EffectiveConfig()
    agent = await db.get(Agent, session.agent_id)
    if agent is None:
        return EffectiveConfig()
    cfg = agent.model_config or {}
    slugs = (
        await db.scalars(
            select(Tool.slug)
            .join(AgentTool, AgentTool.tool_id == Tool.id)
            .where(AgentTool.agent_id == agent.id, Tool.enabled.is_(True))
            .order_by(Tool.slug)
        )
    ).all()
    return EffectiveConfig(
        agent_id=agent.id,
        agent_version=agent.current_version,
        model=cfg.get("model", settings.default_model),
        temperature=float(cfg.get("temperature", 0.7)),
        top_p=float(cfg.get("top_p", 0.9)),
        max_tokens=int(cfg.get("max_tokens", 2048)),
        num_ctx=int(cfg.get("num_ctx", 8192)),
        history_rounds=int(cfg.get("history_rounds", settings.history_rounds)),
        system_prompt=render_prompt(
            agent.system_prompt,
            user_name=user_name,
            session_title=session.title,
            agent_name=agent.name,
        ),
        tool_slugs=list(slugs),
    )
```

`backend/app/ai/runtime.py`：
- `run_generation` 开头（assistant 创建前）解析配置。需要 user_name：从 `db.get(User, session.user_id)` 取（或签名加参数；为避免调用方改动，内部查询）。
- `_load_history` 的 `history_rounds` 改为入参 `rounds: int`。
- `ChatRequest(model=cfg.model, messages=messages, tools=tools_payload(cfg.tool_slugs) or None, temperature=cfg.temperature, top_p=cfg.top_p, max_tokens=cfg.max_tokens, num_ctx=cfg.num_ctx)`。
- 若有 system_prompt：`messages.insert(0, {"role":"system","content":cfg.system_prompt})`（在 history 之前）。
- user 消息与 assistant 消息写入 `agent_id=cfg.agent_id`、assistant `agent_version=cfg.agent_version`、`model=cfg.model`。

`backend/app/schemas/session.py`：
- `SessionCreateIn.agent_id: uuid.UUID | None = None`；`SessionOut.agent_id: uuid.UUID | None`；`MessageOut.agent_id / agent_version`。

`backend/app/services/session_service.py` `create_session`：签名改为 `create_session(db, user, title, agent_id=None)`：
```python
    if agent_id is not None:
        agent = await db.scalar(select(Agent).where(Agent.id == agent_id, Agent.owner_id == user.id))
        if agent is None:
            raise HTTPException(status_code=404, detail="智能体不存在")
        if agent.status == "archived":
            raise HTTPException(status_code=422, detail="该智能体已归档，无法新建会话")
```

`sessions.py` create 透传 `body.agent_id`。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 61 passed：58 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): agent-aware runtime with prompt interpolation"
```

---

### Task 6: 工具调用循环（ReAct，≤5 轮）+ tool_call/tool_result 事件与块

**Files:**
- Modify: `backend/app/ai/runtime.py`（循环、工具执行、块与事件）
- Modify: `backend/tests/fake_provider.py`（多轮脚本 + `requests` 记录）
- Test: `backend/tests/test_tool_loop.py`

**Interfaces:**
- Produces: SSE 新增 `tool_call {message_id, id, name, args}` / `tool_result {message_id, id, status, elapsed_ms, preview}`。
- Produces: blocks 新增 `{type:"tool_call", id, tool, args}` 与 `{type:"tool_result", id, tool, status, elapsed_ms, preview}`（按时间顺序与 text/thinking 混排）。
- 常量：`MAX_TOOL_ROUNDS = 5`；单工具超时 30s；回填模型的结果文本截断 8000 字符；preview 200 字符。

- [ ] **Step 1: 扩展 FakeProvider 并写失败测试**

`backend/tests/fake_provider.py` 调整为「多轮脚本」：构造参数接受 `list[list[tuple[str, dict]]]`（每轮一个脚本）或单脚本；每次 `chat_stream` 取下一轮脚本（耗尽后重复最后一轮）；记录 `self.requests.append(req)` 与 `self.last_request`；保留 `raise_after` 与新参数兼容（`raise_after` 作用于每轮内的事件序号）。

`backend/tests/test_tool_loop.py`：

```python
import json

from app.ai.deps import get_provider
from app.main import app
from tests.fake_provider import FakeProvider

AGENT = {
    "name": "工具助手",
    "system_prompt": "",
    "model_config": {"model": "m"},
    "tags": [],
    "examples": [],
}


async def _session_with_agent(client, auth_headers):
    agent = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()
    s = (
        await client.post("/api/v1/sessions", json={"agent_id": agent["id"]}, headers=auth_headers)
    ).json()
    await client.patch(f"/api/v1/sessions/{s['id']}", json={"title": "t"}, headers=auth_headers)
    return agent, s


async def test_tool_loop_executes_and_persists(client, auth_headers, session_maker):
    from sqlalchemy import select
    from app.models import AgentTool, Tool

    agent, s = await _session_with_agent(client, auth_headers)
    async with session_maker() as db:
        tool = (await db.scalars(select(Tool).where(Tool.slug == "time_now"))).first()
        db.add(AgentTool(agent_id=agent["id"], tool_id=tool.id))
        await db.commit()

    provider = FakeProvider(
        [
            [
                ("tool_call", {"id": "c1", "name": "time_now", "args": "{}"}),
                ("usage", {"prompt_tokens": 5, "completion_tokens": 2, "total_ms": 1}),
            ],
            [("token", {"delta": "现在是"}), ("usage", {"prompt_tokens": 9, "completion_tokens": 3, "total_ms": 1})],
        ]
    )
    app.dependency_overrides[get_provider] = lambda: provider
    r = await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "现在几点"}, headers=auth_headers
    )
    body = r.text
    assert "event: tool_call" in body and "event: tool_result" in body and "event: done" in body
    assert len(provider.requests) == 2
    # 第二轮请求带 assistant tool_calls 与 tool 结果消息
    second = provider.requests[1].messages
    assert any(m.get("role") == "assistant" and m.get("tool_calls") for m in second)
    assert any(m.get("role") == "tool" for m in second)

    msgs = (await client.get(f"/api/v1/sessions/{s['id']}/messages", headers=auth_headers)).json()
    blocks = msgs[1]["blocks"]
    types = [b["type"] for b in blocks]
    assert types == ["tool_call", "tool_result", "text"]
    assert blocks[0]["tool"] == "time_now"
    assert blocks[1]["status"] == "ok" and "20" in blocks[1]["preview"]
    assert blocks[2]["content"] == "现在是"


async def test_unknown_tool_becomes_error_result(client, auth_headers):
    agent, s = await _session_with_agent(client, auth_headers)
    provider = FakeProvider(
        [
            [("tool_call", {"id": "c1", "name": "nope", "args": "{}"})],
            [("token", {"delta": "继续"})],
        ]
    )
    app.dependency_overrides[get_provider] = lambda: provider
    await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    msgs = (await client.get(f"/api/v1/sessions/{s['id']}/messages", headers=auth_headers)).json()
    result_block = [b for b in msgs[1]["blocks"] if b["type"] == "tool_result"][0]
    assert result_block["status"] == "error" and "未知工具" in result_block["preview"]


async def test_tool_loop_capped_at_five_rounds(client, auth_headers):
    from sqlalchemy import select
    from app.models import AgentTool, Tool

    agent, s = await _session_with_agent(client, auth_headers)
    async with session_maker() as db:
        tool = (await db.scalars(select(Tool).where(Tool.slug == "time_now"))).first()
        db.add(AgentTool(agent_id=agent["id"], tool_id=tool.id))
        await db.commit()

    always = [[("tool_call", {"id": "c", "name": "time_now", "args": "{}"})]]
    provider = FakeProvider(always)
    app.dependency_overrides[get_provider] = lambda: provider
    r = await client.post(
        f"/api/v1/sessions/{s['id']}/messages", json={"content": "hi"}, headers=auth_headers
    )
    assert "event: done" in r.text
    assert len(provider.requests) == 5  # 轮数上限
```

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_tool_loop.py -v`
Expected: FAIL（fake_provider 不支持多轮 / runtime 无工具循环）

- [ ] **Step 3: 实现工具循环**

`backend/app/ai/runtime.py` 重构核心段（保留既有 finalize/cancel/节流结构）：

```python
MAX_TOOL_ROUNDS = 5
TOOL_TIMEOUT_S = 30.0
TOOL_RESULT_MAX = 8000
TOOL_PREVIEW_LEN = 200


async def _execute_tool(name: str, args: str) -> tuple[str, str]:
    from app.ai.tools.registry import get_tool

    item = get_tool(name)
    if item is None:
        return f"未知工具：{name}", "error"
    try:
        parsed = json.loads(args) if args else {}
        result = await asyncio.wait_for(item.handler(**parsed), timeout=TOOL_TIMEOUT_S)
        return str(result), "ok"
    except Exception as exc:  # noqa: BLE001
        return f"工具执行失败：{exc}", "error"
```

在 `run_generation` 的流式段外围改为多轮（伪代码，实现时保持 blocks 累加、节流 flush、取消检查与 SSE 兼容）：

```python
        cfg = await resolve_effective_config(db, session, user_name=user_name)
        messages = ([{"role": "system", "content": cfg.system_prompt}] if cfg.system_prompt else [])
        messages += await _load_history(db, session.id, exclude_ids={assistant_id}, rounds=cfg.history_rounds)
        if user_content is not None:
            messages.append({"role": "user", "content": user_content})

        tool_slugs = cfg.tool_slugs
        for round_index in range(MAX_TOOL_ROUNDS):
            req = ChatRequest(
                model=cfg.model,
                messages=messages,
                tools=tools_payload(tool_slugs) or None,
                temperature=cfg.temperature,
                top_p=cfg.top_p,
                max_tokens=cfg.max_tokens,
                num_ctx=cfg.num_ctx,
            )
            tool_calls: list[dict] = []
            async for ev in provider.chat_stream(req):
                ...（既有 token/thinking/usage 处理 + 取消检查 + 节流 flush 不变）
                if ev.type == "tool_call":
                    tool_calls.append(ev.payload)

            if not tool_calls:
                break

            for call in tool_calls:
                blocks.append(
                    {
                        "type": "tool_call",
                        "id": call["id"],
                        "tool": call["name"],
                        "args": call["args"],
                    }
                )
                yield sse(
                    "tool_call",
                    {
                        "message_id": str(assistant_id),
                        "id": call["id"],
                        "name": call["name"],
                        "args": call["args"],
                    },
                )
            for call in tool_calls:
                started = time.monotonic()
                result, status = await _execute_tool(call["name"], call.get("args") or "")
                elapsed = round((time.monotonic() - started) * 1000)
                blocks.append(
                    {
                        "type": "tool_result",
                        "id": call["id"],
                        "tool": call["name"],
                        "status": status,
                        "elapsed_ms": elapsed,
                        "preview": result[:TOOL_PREVIEW_LEN],
                    }
                )
                yield sse(
                    "tool_result",
                    {
                        "message_id": str(assistant_id),
                        "id": call["id"],
                        "status": status,
                        "elapsed_ms": elapsed,
                        "preview": result[:TOOL_PREVIEW_LEN],
                    },
                )
                messages.append({"role": "tool", "content": result[:TOOL_RESULT_MAX]})
            messages.append(
                {
                    "role": "assistant",
                    "content": "",
                    "tool_calls": [
                        {
                            "id": call["id"],
                            "type": "function",
                            "function": {"name": call["name"], "arguments": call["args"]},
                        }
                        for call in tool_calls
                    ],
                }
            )
            # 顺序：先 assistant(tool_calls) 再逐条 tool 结果更贴合协议；实现者需保证
            # 追加顺序为 assistant tool_calls 消息在 tool 结果消息之前（把上面两个 for 调换）
```

> 实现注意：消息追加顺序必须为 `assistant(tool_calls)` → `tool` 结果（各一条）；块与 SSE 顺序为 `tool_call*` → `tool_result*`。实现者按此调整（上面代码块中 `messages.append` 需移动到第一个 `for` 之后、第二个 `for` 之前）。

- [ ] **Step 4: 全量测试 + 提交**

Run: `uv run pytest -v`（期望 64 passed：61 + 3）；`uv run ruff check .`

```powershell
git add backend
git commit -m "feat(backend): react tool loop with typed tool events"
```

---

### Task 7: 前端 M1 遗留清理（per-id 流守卫 / 死代码 / favicon）

**Files:**
- Modify: `frontend/src/stores/chatStream.ts`（append 带 messageId 校验；`clearActive(messageId?)` 带 id 守卫）
- Modify: `frontend/src/components/chat/ChatView.tsx`（事件分发传 `message_id`；finally 用捕获的 streamId）
- Modify: `frontend/src/lib/sse.ts`（删除死代码 `streamChat`）
- Delete: `frontend/public/favicon.svg`
- Test: `frontend/src/stores/__tests__/chatStream.test.ts`（更新 + 新增 2 条）

**Interfaces:**
- Produces: `appendToken(delta, messageId)` / `appendThinking(delta, messageId)`（`active?.id !== messageId` 时忽略）；`clearActive(messageId?)`（给定时仅当 `active.id === messageId` 才清空）。

- [ ] **Step 1: 写失败测试（更新 `chatStream.test.ts`）**

```ts
import { expect, test } from "vitest";
import { useChatStreamStore } from "@/stores/chatStream";

test("不同 messageId 的 append 被忽略", () => {
  const s = useChatStreamStore.getState();
  s.clear();
  s.start("m1", "sess-1");
  s.appendToken("A", "m1");
  s.appendToken("B", "m2"); // 应忽略
  s.appendThinking("C", "m2"); // 应忽略
  const active = useChatStreamStore.getState().active!;
  expect(active.content).toBe("A");
  expect(active.thinking).toBe("");
});

test("clearActive 带 id 守卫：不匹配则不清", () => {
  const s = useChatStreamStore.getState();
  s.clear();
  s.start("m1", "sess-1");
  s.clearActive("m2");
  expect(useChatStreamStore.getState().active?.id).toBe("m1");
  s.clearActive("m1");
  expect(useChatStreamStore.getState().active).toBeNull();
});
```

（既有测试中 `start(id, sessionId)`、`clearActive()` 的调用按新签名同步更新；`appendToken/appendThinking` 补 `messageId` 参数。）

- [ ] **Step 2: 运行确认失败（签名不匹配）**

Run: `pnpm run test -- --run src/stores/__tests__/chatStream.test.ts`
Expected: FAIL

- [ ] **Step 3: 实现**

`chatStream.ts` 关键改动：

```ts
  appendToken: (delta, messageId) =>
    set((s) =>
      s.active && s.active.id === messageId
        ? { active: { ...s.active, content: s.active.content + delta } }
        : s,
    ),
  appendThinking: (delta, messageId) =>
    set((s) =>
      s.active && s.active.id === messageId
        ? { active: { ...s.active, thinking: s.active.thinking + delta } }
        : s,
    ),
  clearActive: (messageId) =>
    set((s) => (!messageId || s.active?.id === messageId ? { active: null } : s)),
```

`ChatView.tsx`：事件分发处 `appendToken(delta, data.message_id)`、`appendThinking(delta, data.message_id)`；发送与重生成用局部 `let streamId: string | undefined` 记录 `message_start` 的 id，`finally` 调用 `clearActive(streamId)`（streamId 未定义时退化为清空任意 active）。

`lib/sse.ts`：删除 `streamChat` 导出及其实现（保留 `parseSSE` 与 `SSEEvent`）。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run: `pnpm run test -- --run`（期望 33 passed：31 + 2）；`pnpm build`

```powershell
git add frontend
git commit -m "chore(frontend): per-id stream guard and m1 cleanup"
```

---

### Task 8: 智能体管理页（列表 + 编辑器：信息 / 提示词+变量 / 模型参数）

**Files:**
- Modify: `frontend/package.json`（`pnpm dlx shadcn@latest add slider textarea badge dropdown-menu` 仅新增组件文件与基础依赖；无新增 npm 直接依赖）
- Create: `frontend/src/api/agents.ts`、`frontend/src/pages/AgentsPage.tsx`、`frontend/src/pages/AgentEditorPage.tsx`、`frontend/src/components/agents/AgentForm.tsx`、`frontend/src/components/agents/__tests__/AgentForm.test.tsx`
- Modify: `frontend/src/App.tsx`（路由 `/agents`、`/agents/new`、`/agents/:agentId`）、`frontend/src/components/sidebar/SessionSidebar.tsx`（底部入口「智能体」链接）

**Interfaces:**
- Produces: hooks `useAgents/useAgent/useCreateAgent/useUpdateAgent/useDeleteAgent/useModels`；类型 `AgentItem`（含 `variables/tool_slugs` 由 T4 提供）。
- Produces: `AgentForm` 组件（受控表单；onSubmit 回传 payload；variables 由本地正则实时提取展示 chips）。
- 验收：能以表单创建/编辑智能体（名称/emoji/描述/标签/提示词/温度/top_p/max_tokens/num_ctx/模型下拉/欢迎语/示例），保存后列表可见。

- [ ] **Step 1: 安装组件并写失败测试**

```powershell
cd D:\code\webAgent\frontend
pnpm dlx shadcn@latest add slider textarea badge dropdown-menu
```

`frontend/src/components/agents/__tests__/AgentForm.test.tsx`：

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { expect, test, vi } from "vitest";
import AgentForm from "@/components/agents/AgentForm";

vi.mock("@/api/agents", () => ({
  useModels: () => ({ data: [{ name: "qwen2.5:7b-instruct-q4_K_M", size_mb: 4700 }] }),
  useTools: () => ({ data: [] }),
}));

test("提示词变量实时提取为 chips", async () => {
  render(<AgentForm initial={{ system_prompt: "你是{{agent.name}}，用户 {{user.name}}" }} onSubmit={vi.fn()} />);
  expect(screen.getByText("agent.name")).toBeInTheDocument();
  expect(screen.getByText("user.name")).toBeInTheDocument();
});

test("提交回传包含名称与温度", async () => {
  const onSubmit = vi.fn();
  render(<AgentForm initial={{ name: "A", model_config: { model: "m", temperature: 0.7 } }} onSubmit={onSubmit} />);
  await userEvent.clear(screen.getByLabelText("名称"));
  await userEvent.type(screen.getByLabelText("名称"), "B");
  await userEvent.click(screen.getByRole("button", { name: "保存" }));
  expect(onSubmit).toHaveBeenCalledWith(expect.objectContaining({ name: "B" }));
});
```

- [ ] **Step 2: 运行确认失败（模块不存在）**

Run: `pnpm run test -- --run src/components/agents/__tests__/AgentForm.test.tsx`
Expected: FAIL

- [ ] **Step 3: 实现**

`frontend/src/api/agents.ts`（要点）：

```ts
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { apiFetch, apiJson } from "@/lib/api";

export interface AgentItem {
  id: string;
  name: string;
  emoji: string;
  description: string | null;
  tags: string[];
  system_prompt: string;
  model_config: Record<string, unknown>;
  welcome_msg: string | null;
  examples: string[];
  status: string;
  current_version: number;
  variables: string[];
  tool_slugs: string[];
  created_at: string;
  updated_at: string;
}

export function useAgents() {
  return useQuery({ queryKey: ["agents"], queryFn: () => apiJson<AgentItem[]>("/api/v1/agents") });
}
export function useAgent(id: string | undefined) {
  return useQuery({
    queryKey: ["agents", id],
    queryFn: () => apiJson<AgentItem>(`/api/v1/agents/${id}`),
    enabled: Boolean(id),
  });
}
export function useModels() {
  return useQuery({
    queryKey: ["models"],
    queryFn: () => apiJson<{ name: string; size_mb: number | null }[]>("/api/v1/models"),
  });
}
export function useCreateAgent() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: (payload: Partial<AgentItem>) =>
      apiJson<AgentItem>("/api/v1/agents", { method: "POST", body: JSON.stringify(payload) }),
    onSuccess: () => qc.invalidateQueries({ queryKey: ["agents"] }),
  });
}

export function useUpdateAgent() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: ({ id, patch }: { id: string; patch: Partial<AgentItem> }) =>
      apiJson<AgentItem>(`/api/v1/agents/${id}`, { method: "PATCH", body: JSON.stringify(patch) }),
    onSuccess: () => qc.invalidateQueries({ queryKey: ["agents"] }),
  });
}

export function useDeleteAgent() {
  const qc = useQueryClient();
  return useMutation({
    mutationFn: async (id: string) => {
      const res = await apiFetch(`/api/v1/agents/${id}`, { method: "DELETE" });
      if (!res.ok) {
        const body = (await res.json().catch(() => ({}))) as { detail?: string };
        throw new Error(body.detail ?? `HTTP ${res.status}`);
      }
    },
    onSuccess: () => qc.invalidateQueries({ queryKey: ["agents"] }),
  });
}

export function useTools() {
  return useQuery({
    queryKey: ["tools"],
    queryFn: () =>
      apiJson<
        { id: string; slug: string; name: string; description: string; category: string; is_system: boolean }[]
      >("/api/v1/tools"),
  });
}
```

`AgentForm.tsx` 要点：受控字段 + `label` 与输入关联（`htmlFor`/`id`）；提示词 textarea 下实时渲染 `variables` chips（正则 `/\{\{\s*([\w.]+)\s*\}\}/g`）；温度/`top_p` 用 Slider（显示数值）、`max_tokens`/`num_ctx` 用 number input；模型下拉来自 `useModels`；「保存」提交（新建时禁用空名称）。

`AgentsPage.tsx`：卡片列表（emoji/名称/标签/状态/版本），「新建智能体」→ `/agents/new`，点击卡片 → `/agents/:id`。

`AgentEditorPage.tsx`：`/agents/new` 用空初始值 + `useCreateAgent`；`/agents/:id` 载入 `useAgent` + `useUpdateAgent`；保存后提示成功；提供「删除」（409 时展示服务端 detail）。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run: `pnpm run test -- --run`（期望 35 passed：33 + 2）；`pnpm build`

```powershell
git add frontend
git commit -m "feat(frontend): agents list page and editor form"
```

---

### Task 9: 工具绑定矩阵 + 发布 / 版本列表 / 回滚 UI

**Files:**
- Create: `frontend/src/components/agents/ToolsMatrix.tsx`、`frontend/src/components/agents/VersionsDrawer.tsx`、对应测试 `ToolsMatrix.test.tsx`
- Modify: `frontend/src/pages/AgentEditorPage.tsx`（集成矩阵、发布按钮、版本抽屉）
- Modify: `backend/app/api/v1/agents.py` + `schemas/agent.py`（**后端补齐**：`AgentOut.tool_slugs`；`PUT /agents/{aid}/tools {slugs}`；见 Step 3）

**Interfaces:**
- Produces: `PUT /api/v1/agents/{aid}/tools`（替换绑定，返回 `{slugs: [...]}`）；`AgentOut.tool_slugs`。
- Produces: 前端 `ToolsMatrix({tools, value, onChange})`、`VersionsDrawer({agentId})`（列表 + 回滚确认）。

- [ ] **Step 1: 后端补齐 + 测试（先写后端测试）**

`backend/tests/test_agents_api.py` 追加：

```python
async def test_update_tools_and_tool_slugs(client, auth_headers, session_maker):
    from sqlalchemy import select

    from app.models import Tool
    from app.services import agent_service

    async with session_maker() as db:
        await agent_service.sync_tools(db)

    aid = (await client.post("/api/v1/agents", json=AGENT, headers=auth_headers)).json()["id"]
    r = await client.put(f"/api/v1/agents/{aid}/tools", json={"slugs": ["time_now"]}, headers=auth_headers)
    assert r.status_code == 200 and r.json()["slugs"] == ["time_now"]
    detail = await client.get(f"/api/v1/agents/{aid}", headers=auth_headers)
    assert detail.json()["tool_slugs"] == ["time_now"]
    # 替换语义
    r2 = await client.put(f"/api/v1/agents/{aid}/tools", json={"slugs": []}, headers=auth_headers)
    assert r2.json()["slugs"] == []
```

后端实现：

`agent_service.py` 追加：

```python
async def set_tools(db: AsyncSession, agent: Agent, slugs: list[str]) -> list[str]:
    await db.execute(delete(AgentTool).where(AgentTool.agent_id == agent.id))
    bound: list[str] = []
    for slug in slugs:
        tool = await db.scalar(select(Tool).where(Tool.slug == slug, Tool.enabled.is_(True)))
        if tool is not None:
            db.add(AgentTool(agent_id=agent.id, tool_id=tool.id))
            bound.append(slug)
    await db.commit()
    return bound
```

`schemas/agent.py`：`AgentOut` 增加 `tool_slugs: list[str] = []`；`class ToolsIn(BaseModel): slugs: list[str] = []`。

`api/v1/agents.py`：把 `_to_out(agent)` 改为异步版并填充绑定（所有调用点加 `await`）：

```python
async def _to_out(db: AsyncSession, agent) -> AgentOut:
    out = AgentOut.model_validate(agent)
    out.variables = agent_service.extract_variables(agent.system_prompt)
    out.tool_slugs = await agent_service._tool_slugs(db, agent.id)
    return out
```

新增端点：

```python
@router.put("/{aid}/tools")
async def set_tools(
    aid: uuid.UUID,
    body: ToolsIn,
    user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    agent = await agent_service.get_owned_agent(db, user, aid)
    bound = await agent_service.set_tools(db, agent, body.slugs)
    return {"slugs": bound}
```

- [ ] **Step 2: 运行后端测试**

Run: `uv run pytest tests/test_agents_api.py -v`（期望 6 passed：5 + 1）；`uv run pytest -v`（全量）

- [ ] **Step 3: 前端实现 + 测试**

`ToolsMatrix.tsx`：网格勾选（工具名/描述/分类），值受控，`onChange(slugs)`；保存按钮调用 `PUT /agents/{aid}/tools`（`useMutation` + invalidate agent）。
`VersionsDrawer.tsx`：`useQuery(["agent-versions", id])` → 版本列表（v/时间/名称快照）；「回滚到此版本」→ confirm → `POST rollback` → invalidate agent + versions + 提示。
`AgentEditorPage`：发布按钮（`POST publish` → 成功 toast「已发布 v{n}」）；展示 `current_version` 与状态徽章。

`ToolsMatrix.test.tsx`：

```tsx
import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { expect, test, vi } from "vitest";
import ToolsMatrix from "@/components/agents/ToolsMatrix";

const TOOLS = [
  { id: "1", slug: "time_now", name: "当前时间", description: "d", category: "system", is_system: true },
  { id: "2", slug: "kb_search", name: "知识库检索", description: "d", category: "knowledge", is_system: true },
];

test("勾选触发 onChange", async () => {
  const onChange = vi.fn();
  render(<ToolsMatrix tools={TOOLS} value={[]} onChange={onChange} />);
  await userEvent.click(screen.getByLabelText("当前时间"));
  expect(onChange).toHaveBeenCalledWith(["time_now"]);
});
```

- [ ] **Step 4: 全量测试与构建 + 提交**

Run（后端）: `uv run pytest -v`；`uv run ruff check .`
Run（前端）: `pnpm run test -- --run`（期望 36 passed：35 + 1）；`pnpm build`

```powershell
git add backend frontend
git commit -m "feat: agent tool bindings and version UI"
```

---

### Task 10: 会话集成（选择智能体新建 / 欢迎语与示例 / 工具卡片渲染）

**Files:**
- Modify: `frontend/src/components/sidebar/SessionSidebar.tsx`（新建任务→智能体下拉：无智能体 + 各智能体）
- Modify: `frontend/src/components/chat/ChatView.tsx`（会话头部展示智能体；空会话欢迎区 + 示例 chips 点击发送）
- Modify: `frontend/src/components/chat/BlockRenderer.tsx`（tool_call/tool_result 卡片）
- Modify: `frontend/src/api/sessions.ts`（`SessionItem.agent_id`、创建会话支持 agent_id）
- Tests: `BlockRenderer.test.tsx`（新增工具卡片 2 条）、`ChatView.test.tsx`（欢迎 chips 渲染 1 条）

**Interfaces:**
- Consumes: `useAgents/useAgent`（T8）、`SessionItem.agent_id`（T5 后端已提供）、tool 事件（T6）。
- Produces: 工具卡片渲染（名称/参数/状态点/耗时/preview 折叠）；空会话时 `agent.welcome_msg` + `examples` chips。

- [ ] **Step 1: 写失败测试**

`BlockRenderer.test.tsx` 追加：

```tsx
test("渲染 tool_call 卡片", () => {
  render(
    <BlockRenderer
      block={{ type: "tool_call", id: "c1", tool: "time_now", args: "{}" }}
    />,
  );
  expect(screen.getByText(/time_now/)).toBeInTheDocument();
});

test("渲染 tool_result 状态与耗时", () => {
  render(
    <BlockRenderer
      block={{
        type: "tool_result", id: "c1", tool: "time_now", status: "ok",
        elapsed_ms: 12, preview: "2026-09-11 星期五",
      }}
    />,
  );
  expect(screen.getByText(/12ms/)).toBeInTheDocument();
  expect(screen.getByText(/2026-09-11/)).toBeInTheDocument();
});
```

`ChatView.test.tsx` 追加（沿用现有 mock 风格；`useSession`/`useAgent`/`useMessages` 均在 mock 内返回数据）：

```tsx
vi.mock("@/api/sessions", async (orig) => ({
  ...(await orig<typeof import("@/api/sessions")>()),
  useSession: () => ({ data: { id: "s1", agent_id: "a1" } }),
  useMessages: () => ({ data: [] }),
}));
vi.mock("@/api/agents", () => ({
  useAgent: () => ({
    data: { id: "a1", name: "代码专家", emoji: "💻", welcome_msg: "贴代码给我", examples: ["帮我 review 这段"] },
  }),
}));

test("空会话展示智能体欢迎语与示例", async () => {
  render(
    <MemoryRouter initialEntries={["/sessions/s1"]}>
      <Routes>
        <Route path="/sessions/:sessionId" element={<ChatView />} />
      </Routes>
    </MemoryRouter>,
  );
  expect(await screen.findByText("贴代码给我")).toBeInTheDocument();
  expect(screen.getByRole("button", { name: "帮我 review 这段" })).toBeInTheDocument();
});
```

> 注：如既有 ChatView 测试已 mock 这两个模块，请合并 mock 定义而不是重复 `vi.mock`（Vitest 同路径只生效一次）。

- [ ] **Step 2: 运行确认失败**

Run: `pnpm run test -- --run src/components/chat/__tests__`
Expected: FAIL

- [ ] **Step 3: 实现**

`BlockRenderer.tsx` 追加分支：

```tsx
  if (block.type === "tool_call") {
    return (
      <div className="my-1 rounded border border-dashed px-3 py-2 text-xs text-muted-foreground">
        🔧 调用工具 <span className="font-mono">{String(block.tool)}</span>
        {block.args && block.args !== "{}" ? <span className="ml-1 opacity-70">{String(block.args)}</span> : null}
      </div>
    );
  }
  if (block.type === "tool_result") {
    const ok = block.status === "ok";
    return (
      <details className="my-1 rounded border px-3 py-2 text-xs">
        <summary className="cursor-pointer text-muted-foreground">
          {ok ? "✅" : "⚠️"} {String(block.tool)} · {String(block.elapsed_ms ?? "")}ms
          {!ok ? " · 失败" : ""}
        </summary>
        <p className="mt-1 whitespace-pre-wrap opacity-80">{String(block.preview ?? "")}</p>
      </details>
    );
  }
```

`ChatView.tsx`：
- 头部：`const { data: agent } = useAgent(session?.agent_id)`（session 来自 `useQuery(["sessions", ...])`? 当前 ChatView 没有会话详情查询——用 `useSessions` 列表中找？更稳：新增 `useSession(id)` hook（GET /sessions/{id}）到 `api/sessions.ts`，ChatView 引用）。空消息 + agent 时渲染欢迎区：emoji + 名称 + `welcome_msg`（无则「开始对话吧」）+ examples chips（点击 → 调用与 Composer 相同的 send）。
- 侧边栏：`useAgents()`；「＋ 新建任务」主按钮（默认无智能体创建）+ 下拉箭头列出智能体（DropdownMenu）；选择后 `createSession.mutateAsync({agent_id})` → navigate。
- `api/sessions.ts`：`SessionItem` 增 `agent_id: string | null`；`useCreateSession` 接受可选 `agentId`；新增 `useSession(id)`。

- [ ] **Step 4: 全量测试与构建 + 提交**

Run: `pnpm run test -- --run`（期望 39 passed：36 + 3）；`pnpm build`

```powershell
git add frontend
git commit -m "feat(frontend): agent-aware sessions with tool cards"
```

---

### Task 11: 预置智能体 seed + README + 端到端验收 + 收尾

**Files:**
- Create: `backend/scripts/seed_agents.py`
- Modify: `README.md`（M2 功能与演示数据）
- （控制器收尾：打标签、推送；实现者不打 tag、不 push）

**Interfaces:**
- Produces: demo 用户下 3 个已发布智能体：`通用助手`（无工具）、`代码专家`（无工具）、`时间管家`（绑定 `time_now`，示例问题「现在几点」）；每个含欢迎语与示例问题。

- [ ] **Step 1: 实现 seed 脚本（幂等：按 owner+name 跳过已存在）**

`backend/scripts/seed_agents.py`：

```python
import asyncio

from sqlalchemy import select

from app.core.db import SessionLocal
from app.models import Agent, AgentVersion, AgentTool, Tool, User
from app.ai.tools import sync_tools

PRESETS = [
    {
        "name": "通用助手",
        "emoji": "🤖",
        "description": "日常问答与写作",
        "tags": ["通用"],
        "system_prompt": "你是一位耐心、准确的通用助手。今天是 {{today}}。",
        "welcome_msg": "你好，我是通用助手，有什么可以帮你？",
        "examples": ["帮我写一封请假邮件", "解释一下什么是 REST"],
        "tools": [],
        "model": "qwen2.5:7b-instruct-q4_K_M",
    },
    {
        "name": "代码专家",
        "emoji": "💻",
        "description": "代码审查与疑难解答",
        "tags": ["开发"],
        "system_prompt": "你是资深工程师，回答代码问题要给出可运行的示例，并指出常见坑。今天是 {{today}}。",
        "welcome_msg": "贴出你的代码或报错，我来帮你。",
        "examples": ["帮我 review 这段 Python", "解释一下这个报错"],
        "tools": [],
        "model": "qwen2.5:7b-instruct-q4_K_M",
    },
    {
        "name": "时间管家",
        "emoji": "⏰",
        "description": "可以调用工具查询当前时间",
        "tags": ["工具"],
        "system_prompt": "你是时间管家。用户问时间时，必须调用 time_now 工具再回答。",
        "welcome_msg": "问我现在几点吧。",
        "examples": ["现在几点？", "今天是星期几？"],
        "tools": ["time_now"],
        "model": "qwen2.5:7b-instruct-q4_K_M",
    },
]


async def main() -> None:
    async with SessionLocal() as db:
        await sync_tools(db)
        user = await db.scalar(select(User).where(User.username == "demo"))
        if user is None:
            print("请先运行 scripts.seed 创建 demo 用户")
            return
        created = 0
        for preset in PRESETS:
            exists = await db.scalar(
                select(Agent).where(Agent.owner_id == user.id, Agent.name == preset["name"])
            )
            if exists is not None:
                continue
            agent = Agent(
                owner_id=user.id,
                name=preset["name"],
                emoji=preset["emoji"],
                description=preset["description"],
                tags=preset["tags"],
                system_prompt=preset["system_prompt"],
                model_config={"model": preset["model"], "temperature": 0.7},
                welcome_msg=preset["welcome_msg"],
                examples=preset["examples"],
                status="published",
                current_version=1,
            )
            db.add(agent)
            await db.flush()
            for slug in preset["tools"]:
                tool = await db.scalar(select(Tool).where(Tool.slug == slug))
                if tool is not None:
                    db.add(AgentTool(agent_id=agent.id, tool_id=tool.id))
            db.add(
                AgentVersion(
                    agent_id=agent.id,
                    version=1,
                    snapshot={
                        "name": preset["name"],
                        "emoji": preset["emoji"],
                        "description": preset["description"],
                        "tags": preset["tags"],
                        "system_prompt": preset["system_prompt"],
                        "model_config": {"model": preset["model"], "temperature": 0.7},
                        "welcome_msg": preset["welcome_msg"],
                        "examples": preset["examples"],
                        "tool_slugs": preset["tools"],
                    },
                    published_by=user.id,
                )
            )
            created += 1
        await db.commit()
        print(f"已创建 {created} 个预置智能体（跳过 {len(PRESETS) - created} 个已存在）")


if __name__ == "__main__":
    asyncio.run(main())
```

- [ ] **Step 2: 运行并验证**

Run: `uv run python -m scripts.seed_agents`（期望「已创建 3 个」）；再跑一次（期望「已创建 0 个」）
Run: `uv run pytest -v`（期望 ≥65 passed）；`uv run ruff check .`
Run（前端）: `pnpm run test -- --run`（期望 ≥39 passed）；`pnpm build`

- [ ] **Step 3: README 更新**

根 `README.md`：
- 功能列表追加：智能体管理（系统提示词/变量/参数/工具绑定/版本发布与回滚）、工具调用（time_now；kb_search M3）、选择智能体对话、欢迎语与示例问题。
- 演示数据小节追加：`uv run python -m scripts.seed_agents`。

- [ ] **Step 4: 端到端验收（控制器执行；实现者只做 API 级自测）**

API 级（真实模型，经 Vite 代理）：
1. 登录 demo → `GET /agents` 可见 3 个预置智能体；
2. 用「时间管家」创建会话 → 发送「现在几点」→ SSE 依次出现 `tool_call(time_now)`、`tool_result(ok)`、`token`、`done`；
3. `GET messages` 的 blocks 顺序为 `tool_call → tool_result → text`，且文本包含时间；
4. 发布/回滚：对任一智能体 `publish` → 改 name → `publish`（v2）→ `rollback {version:1}` → name 复原、工具重绑；
5. 前端手测清单：`/agents` 列表与编辑器保存生效；新建任务下拉可选智能体；时间管家会话出现工具卡片；欢迎语/示例 chips 可点击发送。

- [ ] **Step 5: 提交**

```powershell
git add backend/scripts/seed_agents.py README.md
git commit -m "chore: preset agents seed and m2 docs"
```

> 完成后由控制器打标签 `m2-agents` 并按集成流程推送/PR（同 M0/M1）。

---

## 完成定义（M2 Done）

- [ ] 后端 `uv run pytest -v` ≥ 65 passed；`uv run ruff check .` 干净
- [ ] 前端 `pnpm run test -- --run` ≥ 39 passed；`pnpm build` 通过
- [ ] API 级验收（T11 Step 4）1~4 全过；前端手测清单 5 项全过
- [ ] `seed_agents` 幂等；README 更新
- [ ] 标签 `m2-agents` 已打（控制器）；分支已推送

## 已知延后项（不在 M2 范围，记录备查）

- `kb_search` 真实检索实现（M3 知识库）；`web_search`/`http_request` 工具（M4/M5）
- `@` 多智能体接力与 model_switching 事件（M4）；模型显存调度（M4）
- CANCEL_FLAGS 迁 Redis（多 worker 前，M5）；spans 工具调用观测落库（M5；当前工具耗时在块内）
- `uv.lock` 镜像源归一与 CI 索引（工程债）；设计文档 02/04 同步 tool_slugs/examples 字段（M3 前的文档整理）
- 工具循环超过 5 轮时的用户提示文案（当前静默停止）
- PATCH agent 的 `status` 校验较宽松（可归档/发布任意字符串，前端仅暴露合法值）