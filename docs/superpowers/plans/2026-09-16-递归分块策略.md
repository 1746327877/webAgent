# 递归分块策略 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把知识库摄入从"单一硬切"换成递归切分（标题粗分 + 分隔符细分），并把章节路径贯通到引用展示。

**Architecture:** 解析层用 `ParsedDoc` 声明产物格式（markdown / text）；切分层按格式选 `split_markdown` 或 `split_text`，核心是 `_recursive_split`；切分产物把 `headings` 写进 `chunks.meta`，检索层读取后注入 LLM 上下文与 `citation` SSE；前端引用卡片展示章节路径。

**Tech Stack:** Python 3.12 / FastAPI / SQLAlchemy 2 / pytest / ruff；React 19 / TypeScript / vitest / @testing-library/react。

**Spec:** `docs/设计/17-分块策略.md`

## Global Constraints

- 不新增任何依赖（不引 LangChain）。
- 分隔符常量：`("\n\n", "\n", "。", "！", "？", "；", " ", "")`；最后一项 `""` 表示降到硬切。
- 按**字符数**计长（与现状一致），不做 token 计算。
- 切分失败/为空时沿用现有 `failed` 状态与错误信息口径，不新增错误类型。
- 后端完成门槛：`uv run ruff check .` + `uv run pytest -q` 全绿。
- 前端完成门槛：`npx tsc -b` + `npx vitest run` 全绿。
- 后端测试在 `backend/` 目录执行；前端测试在 `frontend/` 目录执行。

---

### Task 1: 递归切分核心

**Files:**
- Modify: `backend/app/ai/rag/splitter.py`
- Test: `backend/tests/test_splitter.py`（新建）

**Interfaces:**
- Consumes: 无
- Produces:
  - `SEPARATORS: tuple[str, ...]`
  - `_hard_split(block: str, size: int, overlap: int) -> list[str]`（保留现有实现）
  - `_recursive_split(text: str, size: int, overlap: int, separators: Sequence[str]) -> list[str]`
  - `split_text(text: str, size: int = 512, overlap: int = 64) -> list[str]`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_splitter.py
from app.ai.rag.splitter import split_text


def test_short_text_not_split():
    assert split_text("短文本。", size=100) == ["短文本。"]


def test_splits_at_sentence_boundary():
    chunks = split_text("第一句。" * 40, size=50, overlap=0)
    assert len(chunks) >= 3
    assert all(len(c) <= 50 for c in chunks)
    # 句末标点优先：所有块都以句号收尾，不会从句子中间断开
    assert all(c.endswith("。") for c in chunks)


def test_falls_back_to_hard_split():
    chunks = split_text("字" * 250, size=100, overlap=20)
    assert len(chunks) >= 3
    assert all(len(c) <= 100 for c in chunks)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_splitter.py -v`
Expected: FAIL（`test_splits_at_sentence_boundary` 断言长度超限，或 import 报错）

- [ ] **Step 3: 实现**

```python
# backend/app/ai/rag/splitter.py
import re
from collections.abc import Sequence

# 分隔符按"结构强度"从强到弱；"" 表示降到硬切。
# 刻意不含 . , ; ——避免把 v1.2.3 / 小数 / 代码语句从中间切开。
SEPARATORS: tuple[str, ...] = ("\n\n", "\n", "。", "！", "？", "；", " ", "")


def _hard_split(block: str, size: int, overlap: int) -> list[str]:
    chunks, start = [], 0
    while start < len(block):
        chunks.append(block[start : start + size])
        start += max(1, size - overlap)
    return chunks


def _recursive_split(
    text: str, size: int, overlap: int, separators: Sequence[str]
) -> list[str]:
    """按分隔符优先级递归切分，尽量让每块 <= size；分隔符用尽则硬切。"""
    if len(text) <= size:
        return [text]
    if not separators or separators[0] == "":
        return _hard_split(text, size, overlap)

    sep, rest = separators[0], separators[1:]
    parts = text.split(sep)
    # 保留分隔符：除末段外每段补回 sep，保证内容不丢失
    pieces = [p + sep for p in parts[:-1]] + [parts[-1]]

    chunks: list[str] = []
    current = ""
    for piece in pieces:
        if not piece:
            continue
        if len(current) + len(piece) <= size:
            current += piece
            continue
        if current:
            chunks.append(current)
            current = ""
        if len(piece) > size:
            chunks.extend(_recursive_split(piece, size, overlap, rest))
        else:
            current = piece
    if current:
        chunks.append(current)
    return chunks or _hard_split(text, size, overlap)


def split_text(text: str, size: int = 512, overlap: int = 64) -> list[str]:
    """纯文本递归切分（段落 → 行 → 句末标点 → 空格 → 硬切）。"""
    normalized = text.replace("\r\n", "\n").strip()
    if not normalized:
        return []
    return _recursive_split(normalized, size, overlap, SEPARATORS)
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_splitter.py tests/test_rag_pipeline.py -v`
Expected: PASS（含原有 `test_splitter_*` 三个用例）

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/rag/splitter.py backend/tests/test_splitter.py
git commit -m "feat(rag): 递归切分核心，超长块按句末标点优先断"
```

---

### Task 2: Markdown 标题分节

**Files:**
- Modify: `backend/app/ai/rag/splitter.py`
- Test: `backend/tests/test_splitter.py`（追加）

**Interfaces:**
- Consumes: Task 1 的 `_recursive_split`、`SEPARATORS`
- Produces:
  - `_split_markdown_sections(text: str) -> list[tuple[list[str], str]]`（返回 `[(heading_path, section_body)]`）
  - `split_markdown(text: str, size: int = 512, overlap: int = 64) -> list[tuple[str, list[str]]]`（返回 `[(chunk, heading_path)]`）

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_splitter.py（追加）
from app.ai.rag.splitter import split_markdown


def test_markdown_does_not_cross_sections():
    text = "# 第一章\n" + "甲" * 300 + "\n\n# 第二章\n" + "乙" * 300
    chunks = split_markdown(text, size=100, overlap=0)
    first = [c for c, _ in chunks if "甲" in c]
    second = [c for c, _ in chunks if "乙" in c]
    assert first and second
    assert all("乙" not in c for c in first)
    assert all("甲" not in c for c in second)
    assert all(h == ["第一章"] for c, h in chunks if "甲" in c)
    assert all(h == ["第二章"] for c, h in chunks if "乙" in c)


def test_markdown_keeps_heading_line_in_body():
    chunks = split_markdown("# 标题A\n正文内容。", size=100)
    assert len(chunks) == 1
    assert chunks[0][0].startswith("# 标题A")   # 标题词必须进正文，才能进向量
    assert chunks[0][1] == ["标题A"]


def test_markdown_ignores_hash_inside_code_fence():
    text = "# 真标题\n\n```bash\n# 这是代码注释\n```\n\n正文。"
    chunks = split_markdown(text, size=100)
    assert len(chunks) == 1
    assert chunks[0][1] == ["真标题"]


def test_markdown_heading_path_is_nested():
    text = "# 第一章\n\n## 1.1 小节\n\n小节内容。"
    chunks = split_markdown(text, size=100)
    paths = [h for c, h in chunks if "小节内容" in c]
    assert paths == [["第一章", "1.1 小节"]]
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_splitter.py -v`
Expected: FAIL（`ImportError: cannot import name 'split_markdown'`）

- [ ] **Step 3: 实现**

```python
# backend/app/ai/rag/splitter.py（追加）
_HEADING_RE = re.compile(r"^(#{1,6})\s+(.+?)\s*$")
_FENCE_RE = re.compile(r"^\s*(```|~~~)")


def _split_markdown_sections(text: str) -> list[tuple[list[str], str]]:
    """按 Markdown 标题分节，返回 [(heading_path, 小节正文)]。

    规则：标题行并入其小节正文；代码围栏内的 # 不算标题；首个标题前的内容 heading_path=[]。
    """
    sections: list[tuple[list[str], str]] = []
    stack: list[tuple[int, str]] = []
    headings: list[str] = []
    buf: list[str] = []
    in_fence = False

    def flush() -> None:
        body = "\n".join(buf).strip()
        if body:
            sections.append((list(headings), body))
        buf.clear()

    for line in text.split("\n"):
        if _FENCE_RE.match(line):
            in_fence = not in_fence
            buf.append(line)
            continue
        match = None if in_fence else _HEADING_RE.match(line)
        if match is None:
            buf.append(line)
            continue
        flush()
        level = len(match.group(1))
        while stack and stack[-1][0] >= level:
            stack.pop()
        stack.append((level, match.group(2).strip()))
        headings = [title for _, title in stack]
        buf.append(line)
    flush()
    return sections


def split_markdown(
    text: str, size: int = 512, overlap: int = 64
) -> list[tuple[str, list[str]]]:
    """Markdown 递归切分：先按标题分节，再对每节正文细分；片段不跨章节。"""
    normalized = text.replace("\r\n", "\n").strip()
    if not normalized:
        return []
    out: list[tuple[str, list[str]]] = []
    for headings, section in _split_markdown_sections(normalized):
        for piece in _recursive_split(section, size, overlap, SEPARATORS):
            out.append((piece, list(headings)))
    return out
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_splitter.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/rag/splitter.py backend/tests/test_splitter.py
git commit -m "feat(rag): Markdown 标题分节，片段不跨章节且携带章节路径"
```

---

### Task 3: 解析产物声明格式

**Files:**
- Modify: `backend/app/ai/rag/document_parser.py`
- Test: `backend/tests/test_document_parser.py`

**Interfaces:**
- Consumes: `mineru_client.is_enabled()`、`mineru_client.parse_markdown`、`parsers.extract_text`
- Produces: `ParsedDoc(kind: str, pages: list[tuple[int | None, str]])`、`extract_document(path: Path, file_type: str) -> ParsedDoc`；`extract_pages` 删除

- [ ] **Step 1: 改测试为期望新接口**

```python
# backend/tests/test_document_parser.py（整文件替换）
from app.ai.rag import document_parser
from app.ai.rag.document_parser import ParsedDoc
from app.ai.rag.mineru_client import MinerUError
from app.core.config import settings


def test_pdf_uses_mineru_when_enabled(tmp_path, monkeypatch):
    monkeypatch.setattr(settings, "mineru_api_url", "http://mineru.test:8001")
    monkeypatch.setattr(
        document_parser.mineru_client, "parse_markdown", lambda path, file_type: "# MD"
    )
    result = document_parser.extract_document(tmp_path / "a.pdf", "pdf")
    assert result == ParsedDoc(kind="markdown", pages=[(None, "# MD")])


def test_falls_back_when_mineru_fails(tmp_path, monkeypatch):
    monkeypatch.setattr(settings, "mineru_api_url", "http://mineru.test:8001")

    def boom(path, file_type):
        raise MinerUError("service down")

    monkeypatch.setattr(document_parser.mineru_client, "parse_markdown", boom)
    from app.ai.rag import parsers

    monkeypatch.setattr(parsers, "extract_text", lambda path, file_type: [(1, "fallback")])
    result = document_parser.extract_document(tmp_path / "a.pdf", "pdf")
    assert result == ParsedDoc(kind="text", pages=[(1, "fallback")])


def test_falls_back_when_mineru_returns_empty(tmp_path, monkeypatch):
    monkeypatch.setattr(settings, "mineru_api_url", "http://mineru.test:8001")
    monkeypatch.setattr(
        document_parser.mineru_client, "parse_markdown", lambda path, file_type: "   "
    )
    from app.ai.rag import parsers

    monkeypatch.setattr(parsers, "extract_text", lambda path, file_type: [(None, "builtin")])
    result = document_parser.extract_document(tmp_path / "a.pdf", "pdf")
    assert result == ParsedDoc(kind="text", pages=[(None, "builtin")])


def test_disabled_skips_mineru(tmp_path, monkeypatch):
    monkeypatch.setattr(settings, "mineru_api_url", "")

    def should_not_call(path, file_type):
        raise AssertionError("MinerU should not be called when disabled")

    monkeypatch.setattr(document_parser.mineru_client, "parse_markdown", should_not_call)
    from app.ai.rag import parsers

    monkeypatch.setattr(parsers, "extract_text", lambda path, file_type: [(None, "builtin")])
    result = document_parser.extract_document(tmp_path / "a.docx", "docx")
    assert result == ParsedDoc(kind="text", pages=[(None, "builtin")])


def test_markdown_type_never_uses_mineru(tmp_path, monkeypatch):
    monkeypatch.setattr(settings, "mineru_api_url", "http://mineru.test:8001")

    def should_not_call(path, file_type):
        raise AssertionError("MinerU only handles pdf/docx")

    monkeypatch.setattr(document_parser.mineru_client, "parse_markdown", should_not_call)
    path = tmp_path / "note.md"
    path.write_text("# note", encoding="utf-8")
    result = document_parser.extract_document(path, "md")
    assert result == ParsedDoc(kind="text", pages=[(None, "# note")])
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_document_parser.py -v`
Expected: FAIL（`ImportError: cannot import name 'ParsedDoc'`）

- [ ] **Step 3: 实现**

```python
# backend/app/ai/rag/document_parser.py（整文件替换）
"""文档解析入口：PDF/DOCX 优先用 MinerU，未启用或失败时回退内置解析。

对外只暴露 `extract_document`：返回 `ParsedDoc(kind, pages)`，让下游切分层
知道拿到的是 Markdown 还是纯文本，从而选择对应的分块策略。
"""

import logging
from dataclasses import dataclass
from pathlib import Path

from app.ai.rag import mineru_client
from app.ai.rag.mineru_client import MinerUError

logger = logging.getLogger("app.rag.parser")

# 需要 MinerU 能力（含 OCR / 表格 / 公式）的输入类型
MINERU_TYPES = {"pdf", "docx"}

# 解析产物格式：MinerU 输出 Markdown；内置解析输出纯文本
KIND_MARKDOWN = "markdown"
KIND_TEXT = "text"


@dataclass
class ParsedDoc:
    kind: str                              # KIND_MARKDOWN | KIND_TEXT
    pages: list[tuple[int | None, str]]


def extract_document(path: Path, file_type: str) -> ParsedDoc:
    """解析文档为 ParsedDoc；PDF/DOCX 走 MinerU（成功即 Markdown），其余走内置解析。"""
    if file_type in MINERU_TYPES and mineru_client.is_enabled():
        try:
            markdown = mineru_client.parse_markdown(path, file_type)
        except MinerUError as exc:
            logger.warning("MinerU 解析失败，回退内置解析 file=%s error=%s", path.name, exc)
        else:
            if markdown.strip():
                # MinerU 输出整篇 Markdown，无稳定页码 → page=None
                return ParsedDoc(kind=KIND_MARKDOWN, pages=[(None, markdown)])
            logger.warning("MinerU 解析结果为空，回退内置解析 file=%s", path.name)

    from app.ai.rag.parsers import extract_text

    return ParsedDoc(kind=KIND_TEXT, pages=extract_text(path, file_type))
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_document_parser.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/rag/document_parser.py backend/tests/test_document_parser.py
git commit -m "refactor(rag): 解析产物声明格式，extract_pages 改为 extract_document"
```

---

### Task 4: 流水线接线（按格式分派 + chunk_size 生效）

**Files:**
- Modify: `backend/app/ai/rag/pipeline.py`
- Test: `backend/tests/test_rag_pipeline.py`

**Interfaces:**
- Consumes: Task 2 的 `split_markdown`、Task 1 的 `split_text`、Task 3 的 `extract_document`
- Produces: 写入 `chunks.meta = {"page": ..., "headings": [...]}`（text 无 `headings` 键）

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_rag_pipeline.py（追加）
async def test_run_ingest_uses_kb_chunk_size(session_maker, tmp_path):
    from app.ai.rag.pipeline import run_ingest
    from app.models import Chunk, KnowledgeBase

    doc_id, kb_id = await _seed_doc(session_maker, tmp_path, content="段落内容。" * 200)
    async with session_maker() as db:
        kb = await db.get(KnowledgeBase, uuid.UUID(kb_id))
        kb.chunk_size = 100
        kb.chunk_overlap = 0
        await db.commit()

    async def fake_embedder(texts: list[str]) -> list[list[float]]:
        return [[0.0] * 1024 for _ in texts]

    await run_ingest(
        doc_id, embedder=fake_embedder, upload_dir=str(tmp_path), session_factory=session_maker
    )
    async with session_maker() as db:
        chunks = (
            await db.scalars(select(Chunk).where(Chunk.kb_id == uuid.UUID(kb_id)))
        ).all()
        # 1000 字 / 100 字一块 → 远多于默认 512 时的块数
        assert len(chunks) >= 8


async def test_run_ingest_markdown_chunks_carry_headings(session_maker, tmp_path):
    from app.ai.rag.pipeline import run_ingest
    from app.models import Chunk

    content = "# 第一章\n内容甲。\n\n## 1.1 小节\n内容乙。"
    doc_id, kb_id = await _seed_doc(session_maker, tmp_path, content=content)

    async def fake_embedder(texts: list[str]) -> list[list[float]]:
        return [[0.0] * 1024 for _ in texts]

    await run_ingest(
        doc_id, embedder=fake_embedder, upload_dir=str(tmp_path), session_factory=session_maker
    )
    async with session_maker() as db:
        chunks = (
            await db.scalars(select(Chunk).where(Chunk.kb_id == uuid.UUID(kb_id)))
        ).all()
        heading_paths = [c.meta.get("headings") for c in chunks]
        assert ["第一章"] in heading_paths
        assert ["第一章", "1.1 小节"] in heading_paths
```

> 注意：`_seed_doc` 已支持 `content` 参数（见 `tests/test_rag_pipeline.py:30`），无需改动；若文件顶部未导入 `select`，确认已有 `from sqlalchemy import select`。

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_rag_pipeline.py -v`
Expected: FAIL（`chunk_size=100` 未生效导致块数不足；`headings` 键不存在）

- [ ] **Step 3: 实现**

```python
# backend/app/ai/rag/pipeline.py
# 顶部 import 增补
from app.models import Chunk, Document, KnowledgeBase
```

```python
# run_ingest 内，替换解析与切分段落
                from app.ai.rag.document_parser import KIND_MARKDOWN, extract_document
                from app.ai.rag.splitter import split_markdown, split_text

                parsed = await asyncio.to_thread(extract_document, path, doc.file_type)
                meta_pages = doc.meta or {}
                await _set_status(db, doc, "chunking")

                # KB 的 chunk_size/chunk_overlap 真正生效；KB 缺失时回落默认值
                kb = await db.get(KnowledgeBase, doc.kb_id)
                chunk_size = kb.chunk_size if kb is not None else 512
                chunk_overlap = kb.chunk_overlap if kb is not None else 64

                def _split_pages() -> list[tuple[str, dict]]:
                    out: list[tuple[str, dict]] = []
                    for page_no, page_text in parsed.pages:
                        if parsed.kind == KIND_MARKDOWN:
                            for piece, headings in split_markdown(
                                page_text, chunk_size, chunk_overlap
                            ):
                                out.append((piece, {"page": page_no, "headings": headings}))
                        else:
                            for piece in split_text(page_text, chunk_size, chunk_overlap):
                                out.append((piece, {"page": page_no}))
                    return out

                pieces = await asyncio.to_thread(_split_pages)
```

> 改动范围仅限"解析 → 切片"这段：`pages = ...`、`def _split_pages()`、`pieces = ...` 三处，其余（状态机、jieba、嵌入、落库）不动。

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_rag_pipeline.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/rag/pipeline.py backend/tests/test_rag_pipeline.py
git commit -m "feat(rag): 按解析格式分派切分，chunk_size 配置生效并写入 headings"
```

---

### Task 5: 检索与引用注入章节路径

**Files:**
- Modify: `backend/app/ai/rag/retrieval.py`
- Modify: `backend/app/ai/runtime.py:557-578`
- Test: `backend/tests/test_retrieval.py`

**Interfaces:**
- Consumes: `chunks.meta["headings"]`
- Produces: `RetrievedChunk.headings: list[str]`；`format_context` 来源含章节路径；`citation` SSE 块含 `"headings"`

- [ ] **Step 1: 写失败测试**

```python
# backend/tests/test_retrieval.py（追加）
def test_format_context_includes_heading_path():
    from app.ai.rag.retrieval import RetrievedChunk, format_context

    ctx = format_context(
        [
            RetrievedChunk(
                id="c1",
                content="片段",
                source="a.md",
                page=2,
                rrf_score=0.5,
                channel_hits=2,
                headings=["第3章", "3.1 核心参数"],
            )
        ]
    )
    assert "第3章 › 3.1 核心参数" in ctx
```

同时把 `_seed` 的第一个 chunk 补上章节元数据，并在既有 `test_hybrid_search_rrf_and_channel_hits` 末尾追加一行断言：

```python
    # 检索结果带回 meta.headings
    assert by_content["完全语义相关"].headings == ["第3章"]
```

（`_seed` 中该 chunk 改为：`Chunk(..., meta={"page": 1, "headings": ["第3章"]})`）

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_retrieval.py -v`
Expected: FAIL（`RetrievedChunk` 无 `headings` 字段）

- [ ] **Step 3: 实现**

```python
# backend/app/ai/rag/retrieval.py
from dataclasses import dataclass, field


@dataclass
class RetrievedChunk:
    id: str
    content: str
    source: str
    page: int | None
    rrf_score: float
    channel_hits: int
    similarity: float = 0.0
    headings: list[str] = field(default_factory=list)
```

`hybrid_search` 构造结果时补一个字段：

```python
                headings=list(meta.get("headings") or []),
```

`format_context` 改为：

```python
def _source_label(chunk: RetrievedChunk) -> str:
    """来源标签：文件名 [p页码] [› 章节 › 子节]，缺失项自动省略。"""
    label = chunk.source
    if chunk.page:
        label += f" p{chunk.page}"
    if chunk.headings:
        label += " › " + " › ".join(chunk.headings)
    return label


def format_context(chunks: list[RetrievedChunk]) -> str:
    lines = ["[知识库检索结果]"]
    for i, c in enumerate(chunks, 1):
        snippet = c.content[:300].replace("\n", " ")
        lines.append(f"[{i}] (来源: {_source_label(c)}) {snippet}")
    lines.append("回答时请用 [1][2] 形式标注引用；检索结果未覆盖时明确说明。")
    return "\n".join(lines)
```

`runtime.py` 的 citation 块补字段（`citation_blocks` 列表内）：

```python
                    "headings": c.headings,
```

- [ ] **Step 4: 运行测试确认通过**

Run: `uv run pytest tests/test_retrieval.py tests/test_rag_runtime.py -v`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add backend/app/ai/rag/retrieval.py backend/app/ai/runtime.py backend/tests/test_retrieval.py
git commit -m "feat(rag): 检索结果与引用事件携带章节路径"
```

---

### Task 6: 前端展示章节路径

**Files:**
- Modify: `frontend/src/lib/citations.ts`
- Modify: `frontend/src/components/chat/MessageItem.tsx:29-40`
- Modify: `frontend/src/components/chat/CitationList.tsx`
- Modify: `frontend/src/components/chat/ChatView.tsx:531-542`
- Test: `frontend/src/components/chat/__tests__/MessageItem.test.tsx`（追加）

**Interfaces:**
- Consumes: `citation` SSE / 持久化 block 的 `headings: string[]`
- Produces: `Citation.headings?: string[]`

- [ ] **Step 1: 写失败测试**

```tsx
// frontend/src/components/chat/__tests__/MessageItem.test.tsx（追加）
test("引用卡片展示章节路径", () => {
  render(
    <MessageItem
      message={makeMessage({
        blocks: [
          { type: "text", content: "回答 [1]" },
          {
            type: "citation",
            ref: 1,
            chunk_id: "c1",
            source: "java.md",
            page: 12,
            score: 0.8,
            snippet: "片段",
            headings: ["第3章", "3.1 核心参数"],
          },
        ],
      })}
    />,
  );
  expect(screen.getByText(/第3章 › 3\.1 核心参数/)).toBeInTheDocument();
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run src/components/chat/__tests__/MessageItem.test.tsx`
Expected: FAIL（找不到章节路径文本）

- [ ] **Step 3: 实现**

```ts
// frontend/src/lib/citations.ts
export interface Citation {
  ref: number;
  chunk_id: string;
  source: string;
  page: number | null;
  score: number;
  snippet: string;
  /** 章节路径，来自后端 chunk.meta.headings；历史消息可能缺省 */
  headings?: string[];
}
```

```tsx
// frontend/src/components/chat/MessageItem.tsx（toCitation 内追加）
    snippet: String(block.snippet ?? ""),
    headings: Array.isArray(block.headings) ? block.headings.map(String) : [],
```

```tsx
// frontend/src/components/chat/CitationList.tsx（列表项内追加）
              [{c.ref}] {c.source}
              {c.page != null ? ` · p${c.page}` : ""}
              {c.headings?.length ? ` · ${c.headings.join(" › ")}` : ""}
              （{c.score.toFixed(2)}）
```

```tsx
// frontend/src/components/chat/ChatView.tsx（引用抽屉，页码行下方追加）
              {openCitation.headings?.length ? (
                <p className="truncate text-xs text-muted-foreground">
                  {openCitation.headings.join(" › ")}
                </p>
              ) : null}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/components/chat/__tests__/MessageItem.test.tsx` 然后 `npx tsc -b`
Expected: PASS（tsc 无错误）

- [ ] **Step 5: 提交**

```bash
git add frontend/src/lib/citations.ts frontend/src/components/chat/MessageItem.tsx frontend/src/components/chat/CitationList.tsx frontend/src/components/chat/ChatView.tsx frontend/src/components/chat/__tests__/MessageItem.test.tsx
git commit -m "feat(chat): 引用卡片与抽屉展示章节路径"
```

---

### Task 7: 全量验证

- [ ] **Step 1: 后端**

Run（`backend/`）: `uv run ruff check .` 然后 `uv run pytest -q`
Expected: ruff 无输出；pytest 全绿

- [ ] **Step 2: 前端**

Run（`frontend/`）: `npx tsc -b` 然后 `npx vitest run` 然后 `npx oxlint`
Expected: 全部通过

- [ ] **Step 3: 文档同步确认**

确认 `docs/设计/17-分块策略.md` 已存在且 `.\scripts\sync-docs.ps1` 已执行（已在计划开始前完成，输出 `added=1`）。

- [ ] **Step 4: 提交（如有剩余改动）**

```bash
git add -A
git commit -m "chore(rag): 递归分块策略收尾验证"
```
