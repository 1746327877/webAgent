# 向量索引选型

## HNSW

HNSW 构建慢、内存占用大，但查询延迟低、召回率高，是中小规模场景的首选。它的图一旦建好就不再变化，增量数据需要额外的合并策略。

## IVF 与 PQ

IVF 先做聚类，查询时只在少数簇内搜索；配合 PQ 量化能大幅压缩内存占用，适合亿级数据，代价是召回率下降。

## DiskANN

DiskANN 把图放在 SSD 上，用很少的内存服务超大规模索引，代价是强依赖本地盘 IO，云上要注意盘的类型与吞吐。
