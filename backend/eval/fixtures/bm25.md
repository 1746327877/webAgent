# 关键词检索与 BM25

## 词频饱和

BM25 的评分由三部分组成：词频饱和、逆文档频率与文档长度归一化。词频部分用了饱和函数，所以一个词出现十次不会比出现五次重要一倍。

## 逆文档频率

一个词在越多的文档里出现，区分度就越低，权重也越小。"的"、"是"这类词几乎没有区分度，通常直接过滤掉。

## 与向量检索的互补

BM25 对型号、编号、专有名词很敏感，而向量检索擅长"语义相近但用词不同"的情况。两者用 RRF 按名次倒数相加，不需要对齐分数，这也是混合检索默认不开分数加权的原因。
