# JVM 垃圾回收调优速记

## 分代假设

绝大多数对象朝生夕死，因此堆按分代管理：新生代用复制算法，老年代用标记整理或标记清除。

## 停顿时间

G1 把堆切成 Region 并按回收收益排序，用预测模型控制停顿；ZGC 靠着色指针把停顿压到毫秒级，代价是更高的内存开销。

## 与线程的关系

一次 Full GC 会触发全局停顿，所有应用线程被挂起。因此 GC 日志里的长停顿经常被误判成"线程池卡住了"，排查时应该先看 GC 日志再看线程栈。
